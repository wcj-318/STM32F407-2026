# H题第二问与第四问：PE12模式切换、A到B循线与计时显示

## 1. 本版实现范围

当前通用固件在同一个HEX内提供两个运行时竞赛模式：

| 模式 | 小车行为 | 自动结束条件 |
|---|---|---|
| Q2 | 从A点顺时针循线一圈，返回A点停车 | 八路与五路联合识别A点横向启停线 |
| Q4 | 从A点顺时针循线并通过B点 | 不在B点自动停车；PA0人工停车或丢线停车 |

题面第四问写的是“通过B位置”，没有要求停在B点；赛道只在A点设置横向启停线，B点没有可供程序识别的标记。因此本版不使用未经标定的编码器距离或固定时间猜测B点。Q4启动后保持循线并持续显示时间，裁判在车辆通过B点时判定AB用时。

本工程已增加PA1舵机开环上下摆动：Q2或Q4进入循迹RUN后舵机开始往返，DONE、LOST或PA0人工停车后回到中位。当前仍没有把OpenMV球位置发送到STM32，因此该动作不是摆杆闭环控制，滚球稳定在O点±1 cm仍需单独接入视觉反馈并实车验收。

## 2. 按钮接线

| 按钮端 | 连接 |
|---|---|
| 一端 | PE12 |
| 另一端 | GND |

PE12在源码中配置为内部上拉输入：松开为高电平，按下为低电平。不要把按钮另一端接5 V，也不要再把这个外接按钮接到PA0。PA0保留给板载KEY1作为启动/人工停车键。

PE12采用30 ms轮询消抖。一次按住只切换一次；必须松开后再次按下才会产生下一次切换。为防止行驶中改变停车规则，PE12只在WAIT、DONE或LOST状态有效，RUN状态按下会被忽略。

## 3. OLED显示

OLED第0行显示竞赛模式、状态和PA0原始电平，第7行显示时间：

```text
Q2 WAIT K0     T:00.0S
Q2 RUN  K0     T:03.2S
Q2 DONE K0     T:18.6S
Q4 WAIT K0     T:00.0S
Q4 RUN  K0     T:05.4S
```

时间从PA0有效启动且八路存在黑线时清零开始。RUN期间每500 ms更新一次，进入DONE、LOST或PA0人工停车后冻结。显示上限为99.9 s。

## 4. 操作流程

### 4.1 第二问Q2

1. 上电确认OLED为 `Q2 WAIT`、`T:00.0S`。
2. 将小车置于A点，八路对准环形主线，五路位于横向启停线范围。
3. 按一次板载KEY1/PA0，OLED进入 `Q2 RUN`，时间从0开始。
4. 小车离开A点后停车检测武装；返回A点并满足八路/五路联合判据时进入 `Q2 DONE`，电机停止、时间冻结。

### 4.2 第四问Q4的A到B

1. 小车必须处于非RUN状态，按一次PE12，确认OLED从 `Q2 WAIT` 切为 `Q4 WAIT` 且时间清零。
2. 将小车置于A点，钢球置于O点，八路对准主线。
3. 按一次板载KEY1/PA0；OLED进入 `Q4 RUN` 并开始计时。
4. 进入RUN后PA1舵机立即开始上下往返；小车沿上方1.5 m直线顺时针行驶并通过B点。Q4不会使用A点横线停车，也不会在B点自动停车。
5. 现场记录车辆指定测试位置通过B点时的显示时间，要求不超过8 s。
6. 完成观察后按一次PA0人工停车；PWM和STBY归零，时间冻结。

## 5. 架空验收

1. 下载最新通用HEX，上电显示必须为 `Q2 WAIT`。
2. 连按并松开PE12五次，每次只能切换一次，显示依次为Q4、Q2、Q4、Q2、Q4。
3. 按住PE12超过2 s，不能连续切换。
4. 八路有有效黑线时按PA0进入RUN；RUN中按PE12，模式不得改变。
5. 再按PA0，左右PWM归零、STBY拉低、时间冻结，舵机回1500 us中位。
6. 选择Q2启动，在离开起点后模拟八路中间六路至少5路命中且五路中间三路至少1路命中，应进入DONE。
7. 选择Q4重复同一横线输入，状态必须仍为RUN，证明A点停车判据已旁路。

## 6. 落地验收记录

| 次数 | 模式 | A到B时间/s | 是否丢线 | B点后是否仍RUN | 球O点最大误差/cm | 备注 |
|---:|---|---:|---|---|---:|---|
| 1 | Q4 |  |  |  |  |  |
| 2 | Q4 |  |  |  |  |  |
| 3 | Q4 |  |  |  |  |  |

AB时间必须三次均不超过8 s。滚球误差必须使用图传录像按题目刻度核对，不能仅凭编译结果或肉眼瞬时观察填写。

## 7. 构建结果与烧录文件

2026-08-01已完成两套全量构建：

| 构建 | 结果 | HEX SHA256 |
|---|---|---|
| Keil `MDK-ARM/STM32F407/STM32F407.hex` | 0 Error，0 Warning | `500B36A07ED0C32D16CE3F634AA9DA4A809FDDFF08E3C59779FF82EB7A10A1E3` |
| EIDE `build/STM32F407/STM32F407.hex` | Build successfully | `A39CB5ABB1CC7B49795633342AB602F43427CC0F6ED9B6F507F2965254E191E4` |

两套构建的程序尺寸一致：`Code=15054, RO-data=726, RW-data=124, ZI-data=15996`。优先烧录Keil路径的HEX。烧录成功只证明固件写入，不代表AB时间、循迹稳定性、舵机机械方向或滚球O点误差已经实车通过。

## 8. 故障定位

- PE12按下没有切换：先确认车辆不在RUN，再测PE12松开约3.3 V、按下约0 V，并确认按钮GND与STM32共地。
- 一次按下切换多次：检查按钮是否真正松开，若波形抖动超过30 ms再适当增大 `PHASE4_MODE_KEY_DEBOUNCE_MS`，不要先修改循迹参数。
- 按PA0没有启动：先看OLED是否为LOST以及八路 `G:` 是否存在黑线；PA0和PE12功能不同。
- Q4经过A点意外停车：确认烧录的是本页哈希对应的新HEX，OLED必须显示 `Q4 RUN`，不是 `Q2 RUN`。
- Q4到B超过8 s：先检查实际AB路线、轮胎打滑和电池状态，再在保持转向差速关系的前提下同步调整基础速度；每次提速后重新验证弯道不脱线。
- 舵机方向与“上下”相反：交换 `HLINE_SERVO_UP_PULSE_US` 和 `HLINE_SERVO_DOWN_PULSE_US`，不要交换电源极性。
- 钢球不能稳定在O点：当前舵机只是开环上下摆动，没有滚球闭环；应继续完成OpenMV位置输出、控制通信和独立闭环验收，不能通过修改小车循迹PD或速度PI解决。
