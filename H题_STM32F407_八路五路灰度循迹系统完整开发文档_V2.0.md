# H题 STM32F407 八路+五路灰度循迹系统完整开发文档 V2.0（原始方案草案）

> 本文保留最初的功能设想，不是当前可下载固件的操作说明。当前唯一总入口是 `00_当前工程总览_完整引脚与功能说明.md`，当前测试阶段是第四阶段“编码器速度 PI”。板载 KEY1/PA0 是唯一启动暂停键，PB10 模式键、五路自动停车、OpenMV 和滚球机构均未接入当前固件。

## 1 项目目标

实现2026电子设计竞赛H题车载平衡滚球运动控制系统循迹部分。

目标：
- 八路灰度主循迹
- 五路灰度辅助停车
- 编码器速度闭环
- 灰度PID稳定控制
- PA0启动
- 独立按键模式切换
- OLED显示
- JDY-31 + VOFA+调参

---

## 2 控制架构

```
八路灰度
    |
 Error计算
    |
循迹PID
    |
左右目标速度
    |
速度PID
    |
 PWM
    |
 电机
    |
编码器反馈
```

---

# 3 传感器分工

## 八路灰度

作用：
- 主循迹
- 黑线位置计算
- PID误差输入

## 五路灰度

作用：
- 停车线检测
- 十字线检测
- A点确认

不参与高速循迹PID。

---

# 4 按键设计

## PA0启动键

功能：
启动当前模式。

流程：

上电 -> 模式选择 -> PA0启动 -> 执行任务


## 模式切换按键

GPIO：PB10

模式：

MODE0 静态滚球测试

MODE1 AB循迹测试

MODE2 整圈循迹测试

MODE3 指定位置测试


---

# 5 软件结构

```
H_LineCar

Core
 main.c

Drivers
 motor.c
 encoder.c
 gray8.c
 gray5.c
 pid.c
 oled.c
 key.c
 mode.c
 vofa.c
 control.c

Config
 pid_config.h
 mode_config.h
 motor_config.h
```

---

# 6 参数管理

pid_config.h

```c
#define BASE_SPEED 70

#define LINE_KP 18
#define LINE_KI 0
#define LINE_KD 10

#define SPEED_KP 0.8
#define SPEED_KI 0.05
#define SPEED_KD 0

#define STOP_SPEED 15
#define STOP_TIME 50
```

---

# 7 分阶段开发流程

阶段1：
八路灰度测试

阶段2：
固定速度灰度PID

阶段3：
编码器采集、方向和十圈标定

阶段4：
左右轮速度PI

阶段5：
五路停车

阶段6：
整车优化


---

# 阶段1 八路灰度测试

目标：
只测试灰度。

关闭：
- 电机
- PID
- 编码器


OLED：

```
GRAY:
11100100

ERROR:
-2
```


权重：

```
S1 -7
S2 -5
S3 -3
S4 -1
S5 +1
S6 +3
S7 +5
S8 +7
```


通过：
- 数据稳定
- Error方向正确


---

# 阶段2 灰度PID循迹

固定速度，不使用编码器。


控制：

```
Error
 |
PID
 |
左右PWM差
```


初始：

```
BASE_SPEED=40
LINE_KP=18
LINE_KD=10
```


KP：
太小 -> 增加
太大 -> 降低


KD：
弯道冲出 -> 增加
动作迟钝 -> 降低


通过：
低速10圈不脱线。


---

# 阶段3 编码器采集与测速

只采集左右编码器累计计数、周期增量和 counts/s，先完成静止、独立性、正反转、十圈重复性和固定 PWM 稳定性验收。本阶段不闭环修改 PWM。

通过：

- 左右通道无串线；
- 静止计数为 0；
- 正反转符号相反；
- 两次十圈结果基本一致。

---

# 阶段4 编码器速度PI

加入：

```
目标速度
 |
速度PID
 |
PWM
 |
编码器反馈
```


参数：

```
SPEED_KP
SPEED_KI
```


KP：
小 -> 响应慢
大 -> 震荡


KI：
消除长期误差。


通过：

左右速度误差 <5%


---

# 阶段5 五路停车


检测：

black_count >= 3


流程：

```
八路发现宽黑线
        |
五路确认
        |
降低速度
        |
停止
```


测试10次：

要求：

停车误差 <=2cm


---

# OLED显示

运行：

```
MODE:ROUND

SPD:70

ERR:0.5

TIME:15.2
```


---

# VOFA调试变量

发送：

```
mode
gray
error
line_pid
target_speed
left_speed
right_speed
```


---

# 调参顺序

```
速度PID
  |
灰度KP
  |
灰度KD
  |
提高速度
  |
停车优化
```


---

# Codex生成要求

生成完整STM32CubeIDE HAL工程：

必须包含：

- .ioc
- GPIO
- EXTI
- PWM
- Encoder
- UART
- I2C
- OLED
- PID
- 灰度驱动
- 状态机


代码要求：

1. 模块化
2. 每个.c/.h独立
3. 参数宏定义
4. 禁止阻塞delay
5. 支持VOFA
6. 支持逐阶段测试


---

# 最终运行流程

```
上电

↓

选择模式

↓

PA0启动

↓

八路循迹

↓

速度闭环

↓

五路检测停车线

↓

停止

↓

显示结果
```
