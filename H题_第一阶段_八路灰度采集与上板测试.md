# H题第一阶段：八路灰度采集与上板测试

> 历史阶段归档：本文中的 `115200`、文本串口输出和 20 ms 轮询按键只对应第一阶段旧固件。当前第四阶段必须使用 `9600-8-N-1 + JustFloat`，只按板载 KEY1（PA0，EXTI0上升沿）；当前操作以 `00_当前工程总览_完整引脚与功能说明.md` 和第四阶段文档为准。

## 1. 交付边界

本阶段的唯一目标是把循迹输入变成可观察、可重复验证的数据。它对应工程内《H题 STM32F407 八路+五路灰度循迹系统完整开发文档 V2.0》的“阶段1：八路灰度测试”。

2026-07-31 首次上板反馈表明旧版 `G:`/`E:` 输出无规律。复查确认旧代码在切换 AD0/AD1/AD2 后只等待 2 个 CPU 空操作，OLED 软件 I2C 每个边沿也只等待 4 个空操作；在 168 MHz 下不能形成可靠的模块稳定时间和 I2C 时序。本版已用 DWT 周期计数器改为确定的微秒延时，并增加通道内多数采样、连续三次扫描多数判决和 `U:` 不稳定掩码。旧 HEX 不再作为验收固件。

已实现：

- YB-MVX05 八路选通读取；地址 `000` 至 `111` 分别读取 CH1 至 CH8。
- 八路掩码、黑线加权误差和无黑线状态。
- 五路灰度的原始掩码读取，供接线验证；本阶段不把它用于循迹或停车判断。
- PA0 高电平启动、PC5 状态指示、PB2 心跳、USART1 观测和 SSD1306 OLED 观测。
- 10 ms 扫描、100 ms 刷新；每次地址切换等待 20 us，每通道读取 7 次并多数判决，再对连续 3 次扫描多数判决。
- `U:` 标出读取期间或连续扫描之间发生变化的通道；静止验收时必须全部为 0。

本阶段明确没有实现：电机 PWM、驱动器使能、编码器、速度 PID、循迹 PID、停车状态机、摆杆/滚球控制和题目第 1 至第 6 项性能验收。因此，构建通过或本阶段上板通过都不等同于 H 题整车完成。

## 2. 硬件依据与连接

主控是 STM32F407VET6，25 MHz HSE，系统时钟 168 MHz；板载下载接口为 SWD，板载 USB 转串口是 USART1 的 PA9/PA10。YB-MVX05 的图示为 5 V 供电、单路 OUT 与 AD0/AD1/AD2 地址选通；用户确认 OUT 高电平代表黑线。

| 信号 | STM32F407 引脚 | 程序配置 | 用途 |
|---|---|---|---|
| YB-MVX05 OUT | PE8 | 浮空数字输入 | 当前选中 CH 的黑线值，`1=黑` |
| YB-MVX05 AD0 | PE9 | 推挽输出 | 地址 bit0 |
| YB-MVX05 AD1 | PE10 | 推挽输出 | 地址 bit1 |
| YB-MVX05 AD2 | PE11 | 推挽输出 | 地址 bit2 |
| 五路 OUT1..OUT5 | PD8..PD12 | 浮空数字输入 | 原始辅助读数 bit0..bit4 |
| 启动键 | PA0 | 下拉数字输入 | 高电平有效，连续 20 ms 后进入 RUN |
| USART1 TX/RX | PA9/PA10 | 115200, 8-N-1 | 串口助手观测 |
| OLED SCL/SDA | PB8/PB9 | 软件 I2C、开漏输出 | SSD1306 显示 |
| 绿色 LED | PC5 | 推挽输出 | RUN 后置低；沿用原工程板级极性 |
| 蓝色 LED | PB2 | 推挽输出 | 原工程 TIM1 心跳 |

供电和接线顺序：

1. 断电接线；YB-MVX05 接 5 V 和系统公共 GND，OLED 建议接 **3.3 V**，使其 I2C 上拉电平保持为 3.3 V。
2. 用万用表确认 YB-MVX05 的 OUT 高电平。本地资料给出了模块 5 V 供电，但没有给出 OUT 的绝对高电平规格；在把高于 3.3 V 的 OUT 直接接入 PE8 前，必须确认该板载 MCU/该批次引脚的 5 V 容限，或加电平转换。不要把 5 V 上拉直接加到未核验的信号线上。
3. 地址线不交叉：`PE9=AD0`、`PE10=AD1`、`PE11=AD2`。公共地缺失会造成所有通道随机变化，不能以软件补偿。
4. PA13、PA14 和 NRST 保留给 SWD；PH0、PH1 保留给 25 MHz HSE，不得复用。

YB-MVX05 的显示位串总是按 `CH1, CH2, ..., CH8`（即地址 `000` 到 `111`）从左至右输出；它不是 PE8 的时间顺序倒置。面对小车前进方向，程序的假设是 CH1 在左、CH8 在右。若逐通道测试证明物理安装相反，应先改正模块安装方向或明确调整下一阶段的权重方向，不能在未验证前开始 PID 调参。

## 3. 程序结构与算法

| 文件 | 职责 |
|---|---|
| `Bsp/Gray/gray.c` | 配置 PE8..PE11、PD8..PD12；轮询 8 个地址；计算掩码与误差 |
| `Bsp/Gray/gray.h` | `Gray_Sample_t` 数据结构和采样接口 |
| `Bsp/OLED/oled.c` | PB8/PB9 软件 I2C、SSD1306 初始化和 5 行诊断显示；DWT 控制 I2C 半周期为 2 us |
| `Bsp/OLED/oled.h` | OLED 对外显示接口 |
| `Core/Src/main.c` | 10 ms 采样、20 ms 非阻塞启动键确认、100 ms OLED/UART 刷新 |
| `Core/Src/gpio.c`、`Core/Inc/main.h` | PA0 下拉输入定义 |
| `MDK-ARM/STM32F407.uvprojx`、`.eide/eide.yml` | 将两个 BSP 源文件和头文件目录加入两个构建入口 |

运行链路：

```text
PE9/10/11 选通 CH1..CH8
          ↓
PE8 读取当前通道（高=黑） → G:CH1..CH8 掩码
                                  ↓
             Error10 = 10 × Σ(黑线权重) / 黑线数量
                                  ↓
                    OLED + USART1 可观测输出
```

权重固定为 `[-7, -5, -3, -1, +1, +3, +5, +7]`。程序保存的是 `error10`，即误差乘以 10，避免在第一阶段引入浮点或 PID。例如 CH1 黑时为 `-70`，显示为 `-7.0`；CH4、CH5 同时黑时为 `0.0`。八路全白时显示 `E:NOLINE`，不伪造为居中。

`G:` 不是 PE8 的一次瞬时读数。每个地址稳定 20 us 后连续读取 7 次，至少 4 次为高才判为黑；随后使用当前扫描和前两次扫描做逐位多数判决。`U:` 的某一位为 1，表示该通道的 7 次读数不一致，或最近 3 次扫描发生过变化。移动黑纸的瞬间允许 `U:` 短暂为 1；黑纸静止 2 s 后仍不归零则该通道不能验收。

五路显示的 `A:xxxxx` 只是 `PD8..PD12` 的原始电平。当前硬件资料没有给出其黑/白的有效极性，因此本阶段不将其解释为停车线，也不参与误差计算。

OLED 每次更新显示：

```text
H1 WAIT       或 H1 RUN
G:00011000    八路：CH1 到 CH8
U:00000000    八路不稳定标记，静止时应全 0
E:+0.0        或 E:NOLINE
A:00000       五路：OUT1 到 OUT5 原始电平
```

按 PA0 后，`H1 WAIT` 变为 `H1 RUN`，PC5 按原模板的 LED 极性置低，UART 开始周期输出。该键不会启动任何电机，因为本阶段工程没有电机控制代码。

默认 OLED 8 位 I2C 地址为 `0x78`（7 位地址 `0x3C`）。若实物只在 `0x3D` 应答，只需把 `Bsp/OLED/oled.c` 的 `OLED_I2C_ADDRESS` 改为 `0x7A` 后重新执行第 4 节构建；不要同时改 SCL/SDA 或加入硬件 I2C 驱动。

## 4. 已完成的构建验证

在本工程目录执行：

```powershell
& 'D:\APP\keil\UV4\UV4.exe' -b 'MDK-ARM\STM32F407.uvprojx' -j0
```

本次开发已于 2026-07-31 使用 Keil ARM Compiler 5.06 update 7 全量构建。`MDK-ARM/STM32F407/STM32F407.build_log.htm` 的最终结果为：

```text
Program Size: Code=15722 RO-data=1330 RW-data=64 ZI-data=15624
FromELF: creating hex file...
"STM32F407\STM32F407.axf" - 0 Error(s), 0 Warning(s).
```

当前可下载文件为 `MDK-ARM/STM32F407/STM32F407.hex`。构建只证明代码针对 STM32F407VETx 完成编译和链接；尚未连接实车，不能视为上板、传感器或赛道行为验证。

构建前还执行了源级计算核对：从 `gray.c` 的实际权重表计算 `NONE`、`CH1`、`CH4+CH5`、`CH8` 四组输入，结果分别为 `NOLINE`、`-7.0`、`0.0`、`+7.0`；同时确认 Keil XML 源文件表包含 `gray.c` 和 `oled.c`。这验证了已写入的固定映射和项目引用，但不替代第 5 节的传感器实测。

## 5. 上板测试步骤

### 5.1 测试准备

1. 断开电机驱动的电机电源或使能，保留主控、灰度、OLED 和 USB 供电。第一阶段无电机代码，但隔离动力更安全。
2. 检查 BOOT0=0，连接板载 CMSIS-DAP/SWD；串口助手连接板载 USB 转串口，设置 `115200-8-N-1`、无流控。
3. 先用 3.3 V 给 OLED 供电，再给 YB-MVX05 接 5 V；确认两者 GND 与 STM32 GND 共地。
4. 使用 Keil 的 Download 下载第 4 节生成的 HEX/AXF。下载成功不等于程序运行，必须继续后续观察。

### 5.2 启动与观测链路

1. 复位后，OLED 应出现 `H1 WAIT`，PB2 心跳按原工程约 1 s 翻转一次。
2. 串口应先出现：

   ```text
   H1 gray test ready; press PA0 to start telemetry
   ```

3. 按住 PA0 至少 20 ms 后松开。OLED 应切换为 `H1 RUN`，PC5 进入 RUN 指示状态；串口随后每约 100 ms 输出一行，例如：

   ```text
   H1,RUN,G=00011000,U=00000000,E=+0.0,A=00000
   ```

4. 若下载后完全无显示也无串口，先观察 PB2 是否跳变，再分别检查 BOOT0、SWD 下载目标、PC5/PB2、PA9/PA10，最后检查外设接线；不要直接判断为灰度算法错误。

### 5.3 八路逐通道验收

使其他通道保持白底，只用不透光黑纸依次覆盖一个接收头。每次移动黑纸后等待至少 2 s；由于用户已确认 `OUT=1` 是黑线，期望结果如下：

| 黑纸覆盖通道 | 期望 `G:` | 期望 `U:` | 期望 `E:` | 判定 |
|---|---|---|---|---|
| 无黑线 | `00000000` | `00000000` | `NOLINE` | 无线状态正确 |
| CH1 | `10000000` | `00000000` | `-7.0` | 最左权重正确 |
| CH2 | `01000000` | `00000000` | `-5.0` | 地址 001 正确 |
| CH3 | `00100000` | `00000000` | `-3.0` | 地址 010 正确 |
| CH4 | `00010000` | `00000000` | `-1.0` | 左中正确 |
| CH5 | `00001000` | `00000000` | `+1.0` | 右中正确 |
| CH6 | `00000100` | `00000000` | `+3.0` | 地址 101 正确 |
| CH7 | `00000010` | `00000000` | `+5.0` | 地址 110 正确 |
| CH8 | `00000001` | `00000000` | `+7.0` | 最右权重正确 |
| 同时覆盖 CH4、CH5 | `00011000` | `00000000` | `+0.0` | 居中计算正确 |

每一行应连续观察至少 2 s：`G:` 不得跳到其他通道、`U:` 必须稳定为全 0、误差符号不得反转。只要 `U:` 有任意一位持续为 1，就应先检查对应传感器高度、阈值、电源和接线，不能把该行记为通过。此表全部通过，才可宣称八路地址顺序、有效电平和误差方向验证完成。

### 5.4 五路辅助输入连通性

依次遮挡五路模块的 OUT1 至 OUT5 对应感应头，记录 `A:` 的第 1 至第 5 位是否分别变化。此测试只要求“物理通道和 PD8..PD12 一一对应”；黑/白到底是 1 还是 0 必须以实测记录为准，留给第四阶段速度PI通过之后的停车阶段。

### 5.5 记录模板

| 日期/固件时间 | CH1..CH8 单通道全通过 | CH4+CH5 居中 | 五路映射 | PA0 RUN | OLED | UART | 操作者 |
|---|---|---|---|---|---|---|---|
| 2026-07-31 / 第一阶段实测 | 是 | 是 | 是 | 是 | 是 | 是 | 用户 |

只有这张表有实测记录时，才能把“上板验证完成”写入开发日志。

## 6. 故障定位

| 现象 | 优先检查 | 不应做的事 |
|---|---|---|
| `G:00000000` 始终不变 | 5 V、公共地、PE8 与 OUT、AD0..AD2 线序 | 直接开始改权重或 PID |
| 所有通道都是 1 | OUT 被上拉/短接、模块高度/阈值、地线 | 把 `11111111` 当成停车线 |
| `U:` 某位持续为 1 | 对应通道阈值处于临界点、传感器高度不合适、供电或 OUT 抖动 | 用软件强行锁定为 0 或 1 |
| `G:` 仍随机但 `U:` 总为 0 | 用 UART 与 OLED 对照；测量 PE9..PE11 地址和 PE8 OUT，检查 AD 线序 | 继续增加滤波掩盖错误接线 |
| CH 顺序错乱 | PE9/10/11 与 AD0/1/2 是否交叉，按逐通道表复核 | 仅靠反转显示字符串掩盖接线问题 |
| 误差左右相反 | 传感器 CH1..CH8 的物理左右方向 | 在阶段 1 加入电机反向控制 |
| OLED 空白 | OLED 3.3 V/GND、PB8/PB9、默认地址 0x3C | 同时替换成硬件 I2C 和修改多个引脚 |
| 无 UART 文本 | 串口助手 115200-8-N-1、PA9/PA10、USB 转串口 | 将 PA9/PA10 再交给 CubeMX 的 UART 中断配置 |
| PA0 无法进入 RUN | PA0 高电平、按键电路、20 ms 持续时间 | 把 PA0 当低电平有效 |

## 7. 下一阶段入口

阶段 2 只应在本文件第 5.3 的八路测试完整通过后开始：固定低速、用当前 `error10` 接入灰度 PID，并另行验证左右轮方向。五路停车、编码器和 H 题摆杆滚球控制不属于本分支的自动后续动作，必须在获得对应驱动/编码器/摆杆引脚和机构测试条件后另行开发和验收。
