# H题 PA1舵机循迹上下摆动开发测试文档

## 1. 当前功能

PA1配置为TIM5_CH2硬件PWM，输出50 Hz舵机信号。只有当前固件为LINE_TRACK且车辆状态进入RUN时，舵机才开始上下往返；WAIT、DONE、LOST和PA0人工停车状态均保持1500 us中位。

该功能在Q2和Q4模式都有效。它是固定脉宽的开环上下摆动，不读取钢球位置，不能替代题目要求的O点±1 cm滚球闭环。

## 2. 接线

以常见MG90S三线舵机为例，必须按实际线色资料复核：

| 舵机端 | 连接 |
|---|---|
| PWM信号 | PA1 |
| VCC | 独立稳定5 V电源 |
| GND | 5 V电源GND，并与STM32 GND共地 |

禁止使用PA1给舵机供电，也不建议由核心板3.3 V引脚给舵机供电。舵机启动电流会造成电源跌落，可能使STM32复位、OLED黑屏或灰度数据跳变。外部5 V必须与STM32共地后，PA1信号才有共同电平基准。

## 3. PWM与动作参数

TIM5位于APB1，当前定时器时钟84 MHz：

```text
预分频 = 84 - 1
计数频率 = 1 MHz，即1计数=1 us
周期 = 20000 - 1，即20 ms/50 Hz
```

所有可调参数集中在 `Bsp/Servo/servo.h`：

| 宏 | 当前值 | 含义 |
|---|---:|---|
| `HLINE_SERVO_CENTER_PULSE_US` | 1500 us | 停车中位 |
| `HLINE_SERVO_UP_PULSE_US` | 1300 us | 上位置 |
| `HLINE_SERVO_DOWN_PULSE_US` | 1700 us | 下位置 |
| `HLINE_SERVO_SWING_INTERVAL_MS` | 700 ms | 每次换向间隔 |
| `HLINE_SERVO_MIN_PULSE_US` | 1000 us | 软件下限 |
| `HLINE_SERVO_MAX_PULSE_US` | 2000 us | 软件上限 |

“上/下”由舵盘和连杆安装方向决定。如果实物方向相反，只交换UP和DOWN两个宏值。如果行程过大，先缩小为1400/1600 us；如果行程不足并且机械无干涉，再每次增加50 us，禁止一开始直接使用1000/2000 us撞机械限位。

## 4. 状态动作

| 车辆状态 | 电机 | PA1舵机 |
|---|---|---|
| WAIT | 停止 | 1500 us中位 |
| RUN | 循迹运行 | 1300/1700 us，每700 ms切换 |
| DONE | 停止 | 1500 us中位 |
| LOST | 停止 | 1500 us中位 |

进入RUN时先输出1300 us。任何路径调用 `Phase4_Stop()` 时都会同时执行 `Motor_Stop()` 和 `Servo_StopSwing()`，因此丢线、Q2返回A点、PA0人工停车或模式切换都不会让舵机继续摆动。

## 5. 上板测试顺序

1. 第一次测试先拆下舵盘或断开摆杆连杆，车轮架空。
2. 舵机暂不接入，烧录最新HEX；示波器或逻辑分析仪测PA1，上电应为20 ms周期、约1500 us高电平。
3. 让八路灰度检测到有效黑线，按PA0进入RUN；PA1应立即变为约1300 us，700 ms后变为约1700 us，此后往返。
4. 再按PA0，PA1应在一个主循环内回到约1500 us。
5. 模拟丢线进入LOST，PA1同样必须回到1500 us。
6. 断电后连接独立5 V舵机电源并共地，先不安装舵盘，观察轴是否按固定周期往返。
7. 确认方向后在中位安装舵盘和连杆，低风险检查是否撞限位，再落地循迹。

## 6. 故障定位

- PA1没有PWM：确认烧录最新HEX并检查PA1没有被其他模块占用；当前应为TIM5_CH2/AF2。
- 上电舵机不在中位：先测PA1是否约1500 us，再检查舵盘安装角度，不要用修改中心值掩盖安装错误。
- 舵机摆动方向相反：交换UP/DOWN宏，不要交换VCC和GND。
- 舵机抖动或STM32复位：优先检查5 V电源电流能力、舵机电源旁路和共地；硬件PWM本身不依赖主循环刷新。
- RUN时不摆动：确认OLED为Q2/Q4 `RUN`而不是LOST；八路无有效黑线时启动会直接进入LOST并回中。
- 机械撞限位：立即断电，脱开连杆，将1300/1700缩小到1400/1600后重新架空测试。

## 7. 构建结果

2026-08-01完成Keil和EIDE全量构建，`servo.c`均参与编译，两套程序尺寸一致：`Code=15054, RO-data=726, RW-data=124, ZI-data=15996`。

| 构建 | HEX SHA256 |
|---|---|
| Keil `MDK-ARM/STM32F407/STM32F407.hex` | `500B36A07ED0C32D16CE3F634AA9DA4A809FDDFF08E3C59779FF82EB7A10A1E3` |
| EIDE `build/STM32F407/STM32F407.hex` | `A39CB5ABB1CC7B49795633342AB602F43427CC0F6ED9B6F507F2965254E191E4` |

编译和HEX校验只能证明固件产物有效，PA1脉宽、舵机供电、机械方向和真实上下行程仍必须按第5节上板测量。
