# H题 STM32F407 当前工程总览、完整引脚与功能说明

> 本文是当前工程的统一入口，以实际参与 Keil/EIDE 编译的源码为准。旧阶段文档和旧 HEX 只作为历史记录，不能覆盖本文中的当前事实。

## 1. 当前工程结论

- 主控：STM32F407VET6 核心板 V5.5，25 MHz 外部晶振，系统时钟168 MHz。
- 当前开发阶段：第三阶段已实测通过；第四阶段灰度外环加双轮编码器速度PI的最终功能与固件已完成，等待H4T整车实车验收。
- 当前源码默认模式：`HLINE_SPEED_MODE_LINE_TRACK`，运行八路灰度循迹外环和左右轮速度PI内环。
- 板载 `KEY1` 是唯一启动/暂停按钮，对应 STM32 的 `PA0`。
- 板载 `KEY2/NRST` 只负责硬件复位，程序没有修改它。
- 外接到 GND 的按钮不接 PA0，也不参与当前工程。
- 八路灰度负责主循迹；停车采用联合判据：八路中间六颗至少4颗命中，并且五路中间三颗任意1颗命中，单帧成立后短制动100 ms再关闭驱动。
- JDY-31 只负责 UART 透传 VOFA 数据；当前应用没有解析蓝牙下发的控制命令。
- OLED 使用 PB8/PB9 软件 I2C；车辆 RUN 时不连续刷新，避免约60 ms的全屏刷新阻塞20 ms速度环。
- PA1输出TIM5_CH2舵机PWM；车辆RUN时舵机持续在0°～45°间小幅往返，WAIT/LOST/DONE时回0°保持。
- 当前工程没有使用 OpenMV、蜂鸣器、驱动板 ADC 或其他 ADC 功能。

## 2. 唯一有效的工程入口

| 项目 | 当前有效位置 | 说明 |
|---|---|---|
| Keil 工程 | `MDK-ARM/STM32F407.uvprojx` | 当前正式工程入口 |
| EIDE 构建参数 | `build/STM32F407/builder.params` | 与 Keil 使用相同应用源码 |
| 应用入口 | `Core/Src/main.c` | 初始化、状态机、按键、调度、VOFA |
| 引脚公共宏 | `Core/Inc/main.h` | LED、KEY1、电机、编码器、舵机 |
| GPIO初始化 | `Core/Src/gpio.c` | LED、KEY1、方向和STBY |
| 定时器初始化 | `Core/Src/tim.c` | TIM1、TIM2、TIM3、TIM4、TIM5 |
| 当前运行模式 | `Bsp/Motor/motor.h` | 当前检入值为LINE_TRACK最终循迹测试 |

当前 Keil/EIDE 实际编译以下应用模块：

- `Bsp/Gray/gray.c`
- `Bsp/LineFollow/line_follow.c`
- `Bsp/Encoder/encoder.c`
- `Bsp/Motor/motor.c`
- `Bsp/Servo/servo.c`
- `Bsp/OLED/oled.c`
- `Bsp/UART/bsp_UART.c`
- `Core/Src/main.c`、`gpio.c`、`tim.c`、`stm32f4xx_it.c`及 HAL 系统文件

`Openmv/`、`编码/`、各阶段 Markdown、图片和旧 HEX 不参与编译。

## 3. PA0、KEY1和复位键的最终定义

| 项目 | 引脚 | 当前用途 | 电平/触发 | 是否允许改变 |
|---|---|---|---|---|
| 板载 KEY1 | PA0 | 启动/暂停 | 松开0，按下3.3V；EXTI0上升沿 | 当前固定 |
| 板载 KEY2 | NRST | STM32硬件复位 | 按下复位 | 不由软件配置 |
| 外接按钮 | 不连接PA0 | 不参与工程 | 原来按钮接GND，已弃用 | 不再连接 |

启动/暂停动作：

1. 上电后状态为 `WAIT`，PWM为0，STBY为低。
2. 按一次板载 KEY1，PA0产生上升沿中断，主循环切换到 `RUN`。
3. 再按一次板载 KEY1，主循环调用 `Motor_Stop()`，回到 `WAIT`。
4. 每两次有效按下之间设置80 ms保护时间；长按只产生一次上升沿。
5. VOFA `I16`显示PA0原始电平，`I17`显示KEY1中断累计次数。

按键中断只记录事件，不在中断里操作电机、串口或OLED。这样即使OLED或UART发送正在阻塞，按键事件也不会因为主循环没有及时轮询而丢失。

## 4. STM32F407完整活动引脚表

下面列出当前工程实际占用以及必须保留的全部 MCU 引脚。未列出的普通 GPIO 当前没有被应用使用。

### 4.1 A口

| STM32引脚 | 外设/信号 | 方向 | 当前作用 | 配置 |
|---|---|---|---|---|
| PA0 | 板载 KEY1 | 输入 | 启动/暂停 | 下拉，EXTI0上升沿，优先级3 |
| PA1 | 舵机PWM | 输出 | TIM5_CH2控制舵机0°～45°往返 | AF2，50 Hz，1000～1250 us |
| PA5 | 左编码器 A相 | 输入 | TIM2_CH1正交计数 | AF1，内部上拉，数字滤波8 |
| PA9 | USART1_TX | 输出 | 发送VOFA数据到JDY-31 RX | AF7，9600-8-N-1 |
| PA10 | USART1_RX | 输入 | 接收JDY-31 TX；当前应用不解析命令 | AF7，上拉 |
| PA13 | SWDIO | 调试 | 程序下载和调试 | 保留，不接普通模块 |
| PA14 | SWCLK | 调试 | 程序下载和调试 | 保留，不接普通模块 |

注意：`bsp_UART.c`函数头部有一处旧注释写成“PA10/PA11”，但实际初始化代码和当前硬件均是 `PA9/PA10`，应以代码为准。

### 4.2 B口

| STM32引脚 | 外设/信号 | 方向 | 当前作用 | 配置/注意事项 |
|---|---|---|---|---|
| PB2 | 板载蓝色LED/BOOT1复用 | 输出 | 约1秒翻转一次，作为运行心跳 | 运行时GPIO输出；上电启动阶段不要外部强拉 |
| PB3 | 左编码器 B相 | 输入 | TIM2_CH2正交计数 | AF1，内部上拉；当前只使用SWD，不使用完整JTAG |
| PB8 | OLED SCL | 输出 | 软件I2C时钟 | 开漏输出，内部上拉 |
| PB9 | OLED SDA | 双向线 | 软件I2C数据 | 开漏输出，内部上拉 |

### 4.3 C口

| STM32引脚 | 驱动板/板载信号 | 方向 | 当前作用 | 停止状态 |
|---|---|---|---|---|
| PC0 | AIN1/左电机IN1 | 输出 | 左电机方向 | 低 |
| PC1 | AIN2/左电机IN2 | 输出 | 左电机方向 | 低 |
| PC2 | BIN1/右电机IN1 | 输出 | 右电机方向 | 低 |
| PC3 | BIN2/右电机IN2 | 输出 | 右电机方向 | 低 |
| PC4 | STY/STBY | 输出 | 驱动使能；RUN及自动短制动时为高，随后置低 | 低 |
| PC5 | 板载状态LED | 输出 | RUN时点亮，WAIT/LOST时熄灭 | 高 |
| PC6 | 右编码器 A相 | 输入 | TIM3_CH1正交计数 | AF2，内部上拉，数字滤波8 |
| PC7 | 右编码器 B相 | 输入 | TIM3_CH2正交计数 | AF2，内部上拉，数字滤波8 |

代码宏名为 `LED_RED`，但 V5.5 核心板资料将 PC5 标为绿色LED。这是历史命名差异，不代表另占一个红色LED引脚。该LED为低电平点亮。

### 4.4 D口

| STM32引脚 | 模块信号 | 方向 | 当前作用 | 位序/复用 |
|---|---|---|---|---|
| PD8 | 五路灰度 OUT1 | 输入 | 辅助灰度第1路 | `gray5_mask` bit0 |
| PD9 | 五路灰度 OUT2 | 输入 | 辅助灰度第2路 | bit1 |
| PD10 | 五路灰度 OUT3 | 输入 | 辅助灰度第3路 | bit2 |
| PD11 | 五路灰度 OUT4 | 输入 | 辅助灰度第4路 | bit3 |
| PD12 | 五路灰度 OUT5 | 输入 | 辅助灰度第5路 | bit4 |
| PD13 | 左电机PWM/PWA | 输出 | TIM4_CH2，左轮占空比 | AF2，10 kHz |
| PD14 | 右电机PWM/PWB | 输出 | TIM4_CH3，右轮占空比 | AF2，10 kHz |

PD8、PD9已被五路灰度占用，因此当前工程不能同时使用常见的 UART3 PD8/PD9 OpenMV 接法。

### 4.5 E口

| STM32引脚 | YB-MVX05信号 | 方向 | 当前作用 | 说明 |
|---|---|---|---|---|
| PE8 | OUT | 输入 | 读取当前选通灰度通道 | 高电平按黑线处理，无内部上下拉 |
| PE9 | AD0 | 输出 | 八路地址bit0 | 通道地址最低位 |
| PE10 | AD1 | 输出 | 八路地址bit1 | 通道地址bit1 |
| PE11 | AD2 | 输出 | 八路地址bit2 | 通道地址最高位 |

八路通道地址和显示位序：

| 通道 | AD2 AD1 AD0 | `gray8_mask`位 | OLED单点期望 |
|---|---|---|---|
| CH1 | 000 | bit0 | `G:10000000` |
| CH2 | 001 | bit1 | `G:01000000` |
| CH3 | 010 | bit2 | `G:00100000` |
| CH4 | 011 | bit3 | `G:00010000` |
| CH5 | 100 | bit4 | `G:00001000` |
| CH6 | 101 | bit5 | `G:00000100` |
| CH7 | 110 | bit6 | `G:00000010` |
| CH8 | 111 | bit7 | `G:00000001` |

### 4.6 H口、启动和调试保留引脚

| 引脚 | 当前用途 | 说明 |
|---|---|---|
| PH0/OSC_IN | 25 MHz HSE输入 | 系统时钟，不能作为普通GPIO |
| PH1/OSC_OUT | 25 MHz HSE输出 | 系统时钟，不能作为普通GPIO |
| NRST | KEY2/复位 | 只做硬件复位，不属于启动/暂停逻辑 |
| BOOT0 | 启动模式选择 | 正常从Flash启动时保持低 |
| PB2/BOOT1 | 同时连接板载蓝色LED | 运行中作为心跳LED，上电时不要外部驱动 |

## 5. 各模块完整接线

### 5.1 板载KEY1

| 核心板资源 | 连接 |
|---|---|
| KEY1 | 板载已连接PA0，无需外接线 |
| KEY2 | 板载已连接NRST，只负责复位 |

外接到GND的按钮从PA0断开。以后测试启动/暂停只按板载KEY1。

### 5.2 YB-MVX05八路灰度

| 模块端 | STM32/电源端 | 用途 |
|---|---|---|
| GND | GND | 必须共地 |
| OUT | PE8 | 选通通道数字输出 |
| AD0 | PE9 | 地址bit0 |
| AD1 | PE10 | 地址bit1 |
| AD2 | PE11 | 地址bit2 |
| 5V | 5V | 模块供电 |

固件没有单独控制该模块的EN引脚。当前使用模块排针提供的OUT和三位地址进行轮询。

### 5.3 五路灰度

| 模块端 | STM32/电源端 |
|---|---|
| OUT1 | PD8 |
| OUT2 | PD9 |
| OUT3 | PD10 |
| OUT4 | PD11 |
| OUT5 | PD12 |
| 5V | 5V |
| GND | GND |

五路读取为`gray5_mask`并显示/上传；自动停车只使用中间三颗OUT2～OUT4，并与八路中间六颗的判据同时成立后触发。

### 5.4 MD240A/TB6612驱动板A、B两组

| 驱动板信号 | STM32 | 当前对应 |
|---|---|---|
| PWA | PD13/TIM4_CH2 | 左轮PWM，10 kHz |
| AIN1 | PC0 | 左轮方向1 |
| AIN2 | PC1 | 左轮方向2 |
| PWB | PD14/TIM4_CH3 | 右轮PWM，10 kHz |
| BIN1 | PC2 | 右轮方向1 |
| BIN2 | PC3 | 右轮方向2 |
| STY/STBY | PC4 | 总使能，运行及100 ms短制动时高，最终停止低 |
| GND | STM32 GND | 逻辑和动力系统必须共地 |

驱动板上的 `ADC`接口当前没有接入STM32，也没有任何ADC采样代码。`STY`按当前板卡丝印作为STBY使用。电池/VM保持已确认的现有接法，代码修改不涉及更换电池电压。

### 5.5 左右编码器

| 编码器 | 信号 | STM32 | 定时器 |
|---|---|---|---|
| 左轮 | CHA | PA5 | TIM2_CH1 |
| 左轮 | CHB | PB3 | TIM2_CH2 |
| 右轮 | CHA | PC6 | TIM3_CH1 |
| 右轮 | CHB | PC7 | TIM3_CH2 |
| 两轮 | VCC | 编码器规定电源 | 不由GPIO供电 |
| 两轮 | GND | 系统GND | 必须共地 |

当前实测标定值：左轮约1468.95计数/圈，右轮约1471.20计数/圈。左轮前进原始计数为正，右轮前进原始计数为负；速度PI内部把右轮符号转换为正数。

### 5.6 舵机

| 舵机线 | 连接 |
|---|---|
| PWM/信号线 | PA1/TIM5_CH2 |
| VCC | 独立稳定5 V电源，按舵机额定电压供电 |
| GND | 舵机电源GND与STM32 GND共地 |

舵机不可直接由STM32的3.3 V引脚供电。程序采用50 Hz PWM，0°为1000 us、45°为1250 us；进入RUN后每20 ms改变1°，约0.9 s走完单程，到端点立即取反，实现向下、向上连续往返。WAIT、LOST和DONE状态回到0°并继续输出保持脉冲。

### 5.7 JDY-31和VOFA

| JDY-31 | STM32 |
|---|---|
| RXD | PA9/USART1_TX |
| TXD | PA10/USART1_RX |
| GND | GND |
| VCC | 按模块供电要求连接 |

通信参数：`9600 bps、8位数据、无校验、1位停止位`。VOFA使用JustFloat，小端float32，每帧18个float，尾部为 `00 00 80 7F`，发送周期100 ms。

### 5.8 OLED

| OLED | STM32 |
|---|---|
| SCL | PB8 |
| SDA | PB9 |
| GND | GND |
| VCC | 按OLED模块额定电压连接 |

软件使用的8位写地址为 `0x78`，对应常见7位地址 `0x3C`。PB8/PB9是软件I2C GPIO，不是硬件I2C外设。

## 6. 定时器和周期

| 资源 | 配置 | 功能 |
|---|---|---|
| TIM1 | 1 ms更新中断 | 累计约1000次后翻转PB2心跳LED |
| TIM2 | 16位编码器模式TI12 | 左轮AB相计数 |
| TIM3 | 16位编码器模式TI12 | 右轮AB相计数 |
| TIM4_CH2 | 10 kHz PWM | 左轮PWM |
| TIM4_CH3 | 10 kHz PWM | 右轮PWM |
| TIM5_CH2 | 50 Hz PWM，1 us/count | PA1舵机，1000～1250 us对应0°～45° |
| SysTick/HAL_GetTick | 1 ms时间基准 | 10/20/100/500 ms任务调度 |

主循环任务：

| 周期/事件 | 执行内容 |
|---|---|
| 每10 ms | 读取八路和五路灰度；循迹模式计算外环目标PWM |
| RUN时每20 ms | PA1舵机角度改变1°，在0°和45°之间到端取反往返 |
| KEY1上升沿 | 累计按键事件；主循环执行启动或暂停 |
| 每20 ms | 更新编码器速度；速度PI；写入实际PWM |
| 每100 ms | 发送一帧VOFA数据 |
| 每500 ms | 仅WAIT/LOST状态刷新OLED |
| RUN期间 | OLED保持最近一帧，不连续刷新 |

## 7. 八路灰度处理和循迹逻辑

1. AD0/AD1/AD2依次选择CH1到CH8。
2. 每次切换地址等待20 us。
3. 每个通道读取7次，间隔3 us，多数表决决定0/1。
4. 再对连续3帧做二取二表决，得到最终 `gray8_mask`。
5. 任一采样或帧间不一致会写入 `gray8_unstable_mask`。
6. 黑线权重为 `-7,-5,-3,-1,+1,+3,+5,+7`，求有效通道平均值并乘10得到 `error10`。
7. 无任何黑线时 `line_valid=0`。

循迹外环当前参数：

- 周期10 ms；基础请求PWM 330‰；弯道基础最低270‰；转向修正上限保持140‰。
- `Kp=5`，`Kd=7`；转向修正限制为±140‰。
- 中心误差范围±1.0时仍保留±20‰的小修正。
- 每步转向修正变化不超过30‰。
- 丢线后保持最后动作30帧，约300 ms；仍无线则停止并进入LOST。

八路灰度只在 `HLINE_SPEED_MODE_LINE_TRACK`模式决定左右目标；左轮、右轮、双轮固定测试模式虽然仍采集灰度，但不会用它控制电机。

## 8. 电机和速度PI

电机输出单位为千分比：`300`表示30%PWM，`600`表示60%PWM。

- TIM4 PWM频率10 kHz。
- 当前实际输出上限600‰。
- 每20 ms实际PWM最多变化20‰，用于限制突然冲击。
- 参考点：左轮300‰约2325 count/s；右轮300‰约2319 count/s。
- 速度低通滤波系数分母为4。
- PI比例系数为 `1/10`，积分分母为2000。
- PI修正限制为±150‰。
- 固定300‰单轮/双轮测试时，PI输出实际调节范围为150～450‰；600‰只是全局硬上限，不能把“未到600”当成未饱和。
- 请求PWM为0时，该轮PI积分、滤波和输出立即清零。
- 普通暂停或丢线停车时，左右输出归零并将PC4/STBY拉低；自动到站先短制动100 ms，随后同样关闭方向和驱动使能。

`HLINE_SPEED_LEFT_COUNTS_PER_REV_X100`和右轮对应值保存了圈计数标定，但当前速度环直接使用count/s参考值，没有计算RPM或距离。

## 9. 运行状态机

| 状态值 | OLED文字 | 含义 | 电机状态 |
|---|---|---|---|
| 0 | WAIT | 上电等待或人工暂停 | PWM=0，STBY=0 |
| 1 | RUN | 当前模式正在运行 | 按PI结果输出 |
| 2 | DONE | 八路与五路联合确认A点停车线 | 短制动100 ms，随后PWM=0、STBY=0 |
| 3 | LOST | 循迹模式确认丢线 | PWM=0，STBY=0 |

在循迹固件中，如果按KEY1时八路灰度没有有效黑线，程序会直接进入LOST。此时按键已经正常触发，不能把“没有运动”误判成KEY1失效。

舵机只在RUN状态往返；进入WAIT、LOST或DONE时立即回到0°保持。舵机动作不参与灰度循迹、电机差速或速度PI计算。

## 10. 四种固件模式

| 模式 | OLED前缀 | 功能 | 当前产物状态 |
|---|---|---|---|
| LEFT_ONLY | H4L | 只运行左轮，右轮强制0 | 已取得有效RUN数据，当前通用HEX已切离该模式 |
| RIGHT_ONLY | H4R | 只运行右轮，左轮强制0 | 已取得1.16秒有效RUN数据，当前通用HEX已切离该模式 |
| DUAL_FIXED | H4D | 两轮固定300‰目标 | 左右单轮通过后重新编译 |
| LINE_TRACK | Q2 | 灰度外环、双轮速度PI、八路与五路联合停车及计时 | 当前通用 `STM32F407.hex`，用于第二问验收 |

源码中的默认宏目前是LINE_TRACK，因此当前重新编译生成的通用 `STM32F407.hex`运行最终双闭环循迹。若退回单轮或双轮固定模式，必须切换宏后重新编译并记录哈希。

当前有效HEX的SHA256：

| 文件 | SHA256 |
|---|---|
| Keil `MDK-ARM/STM32F407/STM32F407.hex` / Q2短制动版 | `A2B39BFC7067BEB345A307BDB7C9E90FBF8FD88AE888BA922D04E8EA9D23AE06` |
| Keil固定产物 `STM32F407_H2Q2_8C6_4OF6_BRAKE100MS.hex` | `A2B39BFC7067BEB345A307BDB7C9E90FBF8FD88AE888BA922D04E8EA9D23AE06` |
| EIDE `build/STM32F407/STM32F407.hex` / Q2短制动版 | `7A00EB8019B8552E923950CB3F8349E8188EEFC77110D2E191CBC7A32028A7ED` |

两个构建入口使用同一份应用源码，但编译选项不同，因此HEX哈希不同；本轮优先下载上表Keil固定产物。旧的5/6停车版和 `STM32F407_H2Q2_8C6_4OF6_5C3_1OF3_EARLY_STOP.hex` 均作为无主动制动的历史版本保留，不用于本轮4 cm超程修正测试。

## 11. VOFA 19个通道

| 通道 | 含义 | 单位/范围 |
|---|---|---|
| I0 | 状态：WAIT=0、RUN=1、DONE=2、LOST=3 | 状态码 |
| I1 | 八路灰度最终mask | 0..255整数 |
| I2 | 八路不稳定mask | 0..255整数 |
| I3 | 八路是否存在有效黑线 | 0/1 |
| I4 | 八路位置误差 `error10` | -70..+70，除10为权重误差 |
| I5 | 左轮目标速度 | count/s |
| I6 | 右轮目标速度 | count/s |
| I7 | 循迹外环修正 | PWM千分比 |
| I8 | 五路灰度mask | 0..31整数 |
| I9 | 实际编码器更新间隔 | ms，通常20 |
| I10 | 左轮原始速度 | count/s，前进为正 |
| I11 | 右轮归一化前进速度 | count/s，程序已取反 |
| I12 | 左轮滤波速度 | count/s |
| I13 | 右轮滤波速度 | count/s |
| I14 | 左轮实际输出PWM | 0..600‰ |
| I15 | 右轮实际输出PWM | 0..600‰ |
| I16 | PA0/板载KEY1原始电平 | 松开0，按下1 |
| I17 | KEY1上升沿中断累计次数 | 每次有效按下至少增加1 |
| I18 | 本次运行计时 | 秒，DONE/LOST/人工停车后冻结 |

上电时UART初始化还会先发送一次文字初始化报告；随后才连续发送JustFloat帧。VOFA应以尾部 `00 00 80 7F`同步二进制帧。

## 12. OLED每行显示内容

| 行 | 内容 | 示例/含义 |
|---|---|---|
| 0 | 模式、状态、KEY1原始值 | 当前为 `Q2 WAIT K0`、`Q2 RUN K1`、`Q2 DONE K0`或`Q2 LOST K0` |
| 1 | 八路最终mask | `G:00011000` |
| 2 | 八路不稳定mask | `U:00000000` |
| 3 | 线路误差 | `E:+0.0`或`E:NOLINE` |
| 4 | 左右实际PWM百分数 | `L:030 R:000` |
| 5 | 循迹修正百分数 | `C:+000` |
| 6 | 五路mask | `A:00000` |
| 7 | 本次运行时间 | `T:19.8S`，RUN时每500 ms更新，停车后冻结 |

RUN状态不周期刷新整屏，只更新第7行时间，所以松开KEY1后行0可能暂时仍显示K1；VOFA `I16`才是实时按键电平。

## 13. 当前未实现和不能混用的内容

- 八路与五路联合停车线状态机已经实现；十字路口转向未实现。
- 没有圈数统计、赛道终点停车、距离控制或位置环。
- 没有OpenMV视觉控制；`Openmv/`目录不参与编译。
- 已实现PA1舵机开环往返，但没有滚球视觉反馈和舵机闭环平衡控制。
- 没有蜂鸣器GPIO。
- 没有读取MD240A的ADC接口。
- VOFA当前只上传，不通过蓝牙在线修改PI参数或控制启停。
- `STM32F407.ioc`只保留了原始TIM1、LED、HSE和SWD配置；TIM2/3/4/5、PA0中断、灰度、OLED、UART、电机和舵机引脚主要由C源码手工加入。直接用CubeMX重新生成可能覆盖这些手写内容，重新生成前必须备份并逐项合并。
- PC6/PC7已用于右编码器，不能再初始化USART6；PD8/PD9已用于五路灰度，不能直接套用其他工程的UART3/OpenMV接法。

## 14. 当前验证状态和下一步顺序

已确认：

- Keil和EIDE使用相同应用源码，当前工程可全量编译。
- 2026-08-01当前Keil与EIDE构建都已编译并链接`servo.c`，两套HEX均通过Intel HEX校验；这只证明固件产物有效，不等于舵机上板通过。
- 八路和五路灰度引脚及位序已经确认。
- 第三阶段已经通过：左右编码器独立、静止为0、正反符号正确、两次十圈重复、固定PWM速度稳定、循迹连续十圈不丢线。
- PA0源码已配置为板载KEY1的上升沿中断，NRST未改动。

仍需实车确认：

- 左轮RUN截图单点速度误差小于1%；右轮13帧、约1.16秒RUN数据的平均滤波误差约-2.83%，方向、控制周期和PI补偿动作正确。两侧仍缺完整10秒连续记录。
- OLED间歇黑屏的供电/I2C稳定性。
- PA1实际50 Hz波形、舵机0°/45°机械方向、独立5 V供电稳定性和RUN状态连续往返动作。
- 双轮一致性和H4T循迹速度闭环的最终实车结果；当前只能确认功能实现与构建完成，不能代替实车通过。

推荐测试顺序：

1. 外接按钮从PA0断开，只保留板载KEY1。
2. 舵机用独立稳定5 V供电并与STM32共地，信号接PA1；先拆开舵机连杆或确认0°～45°没有机械干涉。
3. 架空车轮，下载当前通用 `STM32F407.hex`（LINE_TRACK），让八路灰度先压住有效黑线，再验收I16、I17和I0。
4. WAIT时确认舵机停在0°；按一次KEY1后，确认舵机约0.9 s从0°向下走到45°，随后取反向上回0°并持续往返。
5. 同时确认两轮都按车辆前进方向转动，`I5/I6、I10/I11、I14/I15`均非0；再按一次确认两路PWM归零且舵机回0°。
6. 架空检查通过后落地，依次测试直线、缓弯、急弯和连续十圈，并保存完整VOFA数据。
7. 若双轮速度不能跟随目标，再退回DUAL或单轮模式定位；不要在同一轮测试中同时修改灰度PD和速度PI。
8. 构建和下载成功不等于实车通过；最终以H4T实车数据、十圈结果和实际使用的HEX哈希为准。
