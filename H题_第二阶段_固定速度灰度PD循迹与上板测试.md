# H题第二阶段：固定速度灰度 PD 循迹与上板测试

> 历史阶段归档：本文中的 `115200`、H2文本帧、轮询PA0、400‰基础PWM和旧HEX只对应第二阶段固件。当前第四阶段使用 `9600-8-N-1 + JustFloat`、板载KEY1/PA0中断和20 ms速度PI，不能按本文下载旧固件。

## 1. 阶段目标与边界

本阶段在已经验收通过的第一阶段基础上，只增加两项功能：

1. 使用 TIM4 输出 10 kHz 双路 PWM，驱动 MD240A/TB6612 的左右电机。
2. 每 10 ms 根据八路灰度位置误差计算左右轮差速，实现固定速度循迹。

本阶段明确不实现：

- 编码器采集与测速（第三阶段）；
- 编码器速度PI（第四阶段）；
- 五路灰度停车判定（第四阶段之后）；
- 丢线自动搜索；
- 起止线计圈、自动停车和 H 题后续机构控制。

文档沿用“灰度 PID”的阶段名称，但当前 `KI=0`，代码实际为 PD 控制。这样不会在没有编码器和实车积分边界的情况下引入积分饱和。

## 2. 已冻结的第一阶段结果

以下内容已经由实物逐通道确认，本阶段没有修改：

| 信号 | STM32F407 引脚 | 固件含义 |
|---|---|---|
| YB-MVX05 OUT | PE8 | 高电平为黑线 |
| YB-MVX05 AD0/AD1/AD2 | PE9/PE10/PE11 | 地址 bit0/bit1/bit2 |
| 五路 OUT1..OUT5 | PD8..PD12 | 原始辅助输入 bit0..bit4 |
| 启动键 | PA0 | 高电平有效 |
| UART1 TX/RX | PA9/PA10 | 115200-None-8-1 |
| OLED SCL/SDA | PB8/PB9 | 软件 I2C |

八路通道顺序仍为 `CH1 -> CH8`，权重仍为：

```text
CH1  CH2  CH3  CH4  CH5  CH6  CH7  CH8
 -7   -5   -3   -1   +1   +3   +5   +7
```

严禁为了修正电机方向而改变八路地址、通道顺序、黑白有效电平或误差权重。电机方向错误只允许在电机方向宏或实际电机接线中单独修正。

## 3. 供电约束

本阶段没有改变电瓶、电机、灰度模块或核心板的供电方式和电压。

本地硬件资料记录：

- MD240A 的 VM 范围为 6～15 V；
- MG513X 电机额定工作电压为 12 V；
- 八路巡线模块使用 5 V；
- STM32F407 逻辑为 3.3 V；
- MD240A 推荐 PWM 为 10 kHz。

继续使用已经确认可工作的现有电瓶和供电连接，不因软件调试随意升压、降压或改接电源。STM32、传感器和 MD240A 必须共地。软件中的 `40%`、`70%` 是 PWM 占空比，不是供电电压设置。

## 4. 第二阶段电机引脚

| MD240A/TB6612 信号 | STM32F407 引脚 | 固件宏 | 配置 |
|---|---|---|---|
| 左电机 PWM | PD13 | `HLINE_MOTOR_LEFT_PWM_Pin` | TIM4_CH2 |
| 右电机 PWM | PD14 | `HLINE_MOTOR_RIGHT_PWM_Pin` | TIM4_CH3 |
| 左电机 IN1 | PC0 | `HLINE_MOTOR_LEFT_IN1_Pin` | 推挽输出 |
| 左电机 IN2 | PC1 | `HLINE_MOTOR_LEFT_IN2_Pin` | 推挽输出 |
| 右电机 IN1 | PC2 | `HLINE_MOTOR_RIGHT_IN1_Pin` | 推挽输出 |
| 右电机 IN2 | PC3 | `HLINE_MOTOR_RIGHT_IN2_Pin` | 推挽输出 |
| STBY | PC4 | `HLINE_MOTOR_STBY_Pin` | 运行为高，停车为低 |

PD13、PD14 与 PD8～PD12 不重叠，因此没有占用已经验收的五路灰度输入。

## 5. PWM 计算

系统时钟为 168 MHz，APB1 分频为 4。STM32F407 在 APB 预分频不为 1 时，定时器时钟为 APB 时钟的 2 倍，因此：

```text
APB1 = 168 MHz / 4 = 42 MHz
TIM4 clock = 42 MHz * 2 = 84 MHz
PSC = 0
ARR = 8399
PWM = 84 MHz / (0 + 1) / (8399 + 1) = 10 kHz
```

控制器和串口中的左右 PWM 使用千分比：

```text
400 = 40.0%
700 = 70.0%
```

TIM4 的比较值由千分比换算，控制输出被限制在 `0～700`，所以第二阶段不会命令电机反转，也不会超过 70% 占空比。

## 6. 控制参数和公式

参数集中在 `Bsp/LineFollow/line_follow.h`：

```c
#define HLINE_CONTROL_PERIOD_MS          10U
#define HLINE_BASE_PWM_PERMILLE          400
#define HLINE_MAX_PWM_PERMILLE           700
#define HLINE_LINE_KP                    18
#define HLINE_LINE_KD                    10
```

`error10` 是第一阶段已经验收的误差值乘以 10，例如：

```text
E=-1.0 -> error10=-10
E=+7.0 -> error10=+70
```

每次控制计算：

```text
delta_error10 = error10 - previous_error10
correction = KP * error10 + KD * delta_error10
left_pwm  = clamp(BASE + correction, 0, 700)
right_pwm = clamp(BASE - correction, 0, 700)
```

因此：

- 黑线在左侧，误差为负，左轮减速、右轮加速，车辆应向左修正；
- 黑线在右侧，误差为正，左轮加速、右轮减速，车辆应向右修正；
- CH4、CH5 同时检测到黑线，误差为 0，两轮都是 40%。

刚发生通道跳变的 10 ms 周期中，`KD` 会产生瞬态修正。验收稳态值前，应让黑纸保持不动至少 100 ms，并确认 `U:00000000`。

## 7. 软件结构

| 文件 | 第二阶段职责 |
|---|---|
| `Core/Inc/main.h` | 定义 PC0～PC4、PD13、PD14 电机引脚宏 |
| `Core/Src/gpio.c` | 上电先将 IN1/IN2/STBY 全部置低，再配置为输出 |
| `Core/Inc/tim.h`、`Core/Src/tim.c` | 初始化 TIM4_CH2/CH3 为 10 kHz PWM |
| `Bsp/Motor/motor.h/.c` | PWM 换算、方向输出、STBY 和立即停车 |
| `Bsp/LineFollow/line_follow.h/.c` | 10 ms 固定速度 PD 差速计算和限幅 |
| `Bsp/Gray/gray.c` | 保持第一阶段的八路多数采样、误差和五路原始采集 |
| `Bsp/OLED/oled.c` | 显示第二阶段状态、灰度、误差和电机输出 |
| `Core/Src/main.c` | PA0 启停、状态机、周期调度、OLED/UART 上报 |

Keil 和 EIDE 工程均已加入 `motor.c`、`line_follow.c` 以及对应头文件目录。

当前 `STM32F407.ioc` 是原始 TIM1 示例模板，没有记录第一阶段手工初始化的灰度/OLED/UART，也没有记录本阶段手工加入的电机引脚和 TIM4。现阶段应以本节列出的 C 源码和 Keil 工程为准，不要直接在 CubeMX 中重新生成并覆盖工程。若后续确需重新生成，必须先把 PE8～PE11、PD8～PD14、PC0～PC4、PA0、PA9/PA10、PB8/PB9 的完整配置迁入 `.ioc`，再逐项复验。

## 8. 状态机与安全行为

```text
上电
  |
  v
H2 WAIT --PA0且有线--> H2 RUN --无黑线--> H2 LOST
   ^                      |                    |
   |------PA0停车---------|-------PA0且有线----|
```

三种状态的硬件行为：

| 状态 | STBY | 左右 PWM | 含义 |
|---|---:|---:|---|
| `H2 WAIT` | 0 | 0/0 | 上电等待或人工停车 |
| `H2 RUN` | 1 | PD 计算值 | 固定速度循迹 |
| `H2 LOST` | 0 | 0/0 | 无黑线，锁存停车 |

`LOST` 是锁存状态。黑线重新出现后不会自行启动，必须确认车辆位置安全，再按一次 PA0。这个设计用于第二阶段低速调试，防止丢线后车辆继续直冲。

`U:` 继续作为传感器稳定性诊断，不直接触发停车。车辆移动时通道边缘切换会令 `U:` 短暂为 1；控制器使用第一阶段已经做三次扫描多数判决后的 `G:`。如果车辆静止时 `U:` 长期不为 0，应先修复传感器或接线，不得用调 PID 掩盖。

## 9. OLED 和 UART

OLED 八行含义：

```text
H2 WAIT / H2 RUN / H2 LOST
G:00011000
U:00000000
E:+0.0
L:040 R:040
C:+000
A:00000
B40 P18 D10
```

- `G:`：CH1～CH8，`1=黑`；
- `U:`：八路不稳定掩码；
- `E:`：位置误差；
- `L/R:`：左右 PWM 百分数；
- `C:`：PD 修正量的百分数；
- `A:`：五路灰度原始值，只观察，不参与控制；
- 最后一行：基准 40%、KP=18、KD=10。

UART1 每约 100 ms 输出：

```text
H2,RUN,G=00011000,U=00000000,E=+0.0,L=400,R=400,C=0,A=00000
```

`L/R/C` 均为千分比单位。必须以 `H2` 开头的新格式判断当前烧录的是第二阶段固件；若仍输出 `H1`，说明下载的不是本次生成的 HEX。

## 10. 分步上板测试

所有电机测试先架空车体，使两个驱动轮完全离地。每一步通过后再做下一步。

### 步骤 1：断电接线核对

1. 断开电瓶。
2. 按第 4 节逐根核对 PD13、PD14、PC0～PC4。
3. 确认 PD12 仍接五路灰度 OUT5，没有被接到电机 PWM。
4. 确认 STM32、MD240A 和灰度模块共地。
5. 不改变已经确认的电瓶电压和模块供电。

通过标准：无短路；电机引脚与灰度引脚无复用；STBY 接 PC4。

### 步骤 2：下载和上电静止

下载：

```text
MDK-ARM/STM32F407/STM32F407.hex
```

上电后不要按 PA0。

通过标准：

- OLED 显示 `H2 WAIT`；
- UART 出现 `H2,WAIT,...,L=0,R=0,C=0,...`；
- 两轮完全不动；
- 万用表测 PC4 为低电平；
- PB2 心跳灯约每秒翻转一次。

若上电即转，立即断开电瓶，检查下载文件、PC4 接线和 MD240A STBY 是否接错；不要先改 PID。

### 步骤 3：PWM 频率和居中输出

1. 仍保持车轮离地。
2. 让 CH4、CH5 对准黑线，其余通道为白，等待 `G:00011000`、`U:00000000`、`E:+0.0`。
3. 按 PA0 一次。
4. 用示波器测 PD13、PD14。

通过标准：

- 状态为 `H2 RUN`；
- PD13 和 PD14 均为 `10.0 kHz`，允许测量仪器误差；
- 两路占空比均约 40%；
- OLED 为 `L:040 R:040`；
- UART 为 `L=400,R=400,C=0`。

再次按 PA0 后必须回到 `H2 WAIT`，PC4 变低、两轮停止。

### 步骤 4：左右轮正方向

1. 保持 CH4+CH5 居中。
2. 架空车轮后按 PA0。
3. 从车辆正常前进方向观察，确认两轮共同产生向前运动。

默认正转逻辑为左右电机 `IN1=1, IN2=0`。如果只有某一侧反转，只修改 `Bsp/Motor/motor.h` 中对应的一行：

```c
#define HLINE_MOTOR_LEFT_FORWARD_IN1_STATE  GPIO_PIN_SET
#define HLINE_MOTOR_RIGHT_FORWARD_IN1_STATE GPIO_PIN_SET
```

把错误一侧由 `GPIO_PIN_SET` 改成 `GPIO_PIN_RESET`，重新编译和下载。不要同时交换灰度通道、PD13/PD14 或左右控制公式。

通过标准：`L=400,R=400` 时车辆产生直行方向，不能原地旋转，也不能一前一后。

### 步骤 5：转向符号和稳态输出

车轮继续离地。每次移动黑纸后等待至少 100 ms，确认 `U:00000000` 再记录：

| 黑纸位置 | `G:` | `E:` | 稳态 `L/R` | 期望动作 |
|---|---|---:|---:|---|
| CH1 | `10000000` | `-7.0` | `0/700` | 强左修正 |
| CH2 | `01000000` | `-5.0` | `0/700` | 左修正 |
| CH3 | `00100000` | `-3.0` | `0/700` | 左修正 |
| CH4 | `00010000` | `-1.0` | `220/580` | 小幅左修正 |
| CH4+CH5 | `00011000` | `+0.0` | `400/400` | 居中直行 |
| CH5 | `00001000` | `+1.0` | `580/220` | 小幅右修正 |
| CH6 | `00000100` | `+3.0` | `700/0` | 右修正 |
| CH7 | `00000010` | `+5.0` | `700/0` | 右修正 |
| CH8 | `00000001` | `+7.0` | `700/0` | 强右修正 |

表中为误差稳定后的比例项结果。移动黑纸瞬间，KD 会使 `C` 和 `L/R` 暂时不同，这是预期现象。

通过标准：左侧黑线始终表现为左轮慢、右轮快；右侧相反；居中为两轮相等。

### 步骤 6：丢线保护

1. 在 `H2 RUN` 状态移走黑线，使 `G:00000000`。
2. 观察 OLED、UART、车轮和 PC4。

通过标准：

- OLED 进入 `H2 LOST`；
- `E:NOLINE`；
- UART 为 `H2,LOST,...,L=0,R=0,C=0,...`；
- PC4 为低；
- 两轮立即停止；
- 重新放回黑线后仍保持停止；
- 有线时按一次 PA0 才重新进入 `RUN`。

### 步骤 7：低速落地

前六步全部通过后才能把车放到地面：

1. 先在直线中央放车，传感器高度保持第一阶段验收时的高度。
2. 按 PA0，先测试短直线。
3. 再测试缓弯和急弯。
4. 最后连续运行 10 圈。

第二阶段最终通过标准：

- 连续低速 10 圈不脱线；
- 左右转向符号始终正确；
- 中线附近无持续高频摆动；
- 一旦无黑线，进入 `LOST` 并停车；
- 五路 `A:` 变化不影响本阶段电机控制。

## 11. 调参顺序

只有第 10 节步骤 1～6 全部通过，才允许调参。

1. 保持 `BASE=40%`、`KD=0`，只调 `KP`，先让车能跟线。
2. 车辆中心附近左右高频摆动，减小 `KP`；转向迟钝、容易冲出，增大 `KP`。
3. 恢复并调整 `KD`。入弯过冲可适当增大；动作尖锐、抖动明显则减小。
4. KP、KD 一次只改一个，每次记录直线、缓弯、急弯结果。
5. 只有控制稳定后才小幅调整 `BASE`；不得通过改变电瓶电压代替软件调参。

调参记录：

| 次数 | BASE | KP | KD | 直线 | 缓弯 | 急弯 | 10 圈 | 备注 |
|---:|---:|---:|---:|---|---|---|---|---|
| 1 | 40% | 18 | 10 | 待实测 | 待实测 | 待实测 | 待实测 | 初始固件 |

## 12. 故障定位

| 现象 | 优先检查 | 不应先做 |
|---|---|---|
| 上电即转 | PC4、下载的 HEX、STBY 接线 | 改灰度电平 |
| OLED 为 H1 | 下载了旧第一阶段固件 | 调电机线 |
| 两轮都不转 | `H2` 状态、PC4、VM、共地、PD13/14 PWM | 提高电瓶电压 |
| 一侧不转 | 对应 PWM/IN1/IN2、电机端子、驱动通道 | 改 KP/KD |
| 居中原地转 | 某侧正转方向宏或电机端子 | 反转灰度权重 |
| 左线却向右修正 | 先看 UART 的 E、L、R；若 E 正负正确，再查左右电机定义 | 交换 AD0/AD1/AD2 |
| 频繁 LOST | `G:` 是否确为 0、传感器高度和线宽、供电与共地 | 增大基础速度 |
| G 正确但车辆摆动 | 逐项调 KP/KD | 改已验收通道顺序 |
| U 静止时不归零 | 传感器 OUT、地址线、共地、干扰 | 用更大滤波掩盖硬件问题 |

## 13. 当前验证状态

已经完成的软件侧验证：

- TIM4 计算结果为 10 kHz；
- PD13/PD14、PC0～PC4 与灰度引脚无冲突；
- CH1～CH8 和 CH4+CH5 的稳态左右输出已按公式逐项计算；
- Keil 全量编译实际包含 `motor.c` 和 `line_follow.c`；
- 工程 XML 可解析；
- 2026-07-31 06:10:52 完成 Keil 全量重编译，为 0 Error、0 Warning；
- 程序尺寸为 `Code=18086, RO-data=1422, RW-data=76, ZI-data=15700`；
- 已生成新的 AXF 和 HEX。

本阶段当时的历史 HEX（不用于当前第四阶段）：

```text
路径：MDK-ARM/STM32F407/STM32F407.hex
大小：55131 bytes
SHA-256：D550D5132B49FE127FB78F4FED524FC1A6C77320F7834F7114F85A1701CF1510
```

后续实测已经确认左右轮能够正转并完成连续十圈循迹；第三阶段的 `顺时针` 记录又完成了一次连续十圈回归。以下电气测量记录仍未归档：

- PD13/PD14 的示波器实测频率和占空比；
- MD240A 实际 STBY 电平波形。

第二阶段循迹功能按后续连续十圈结果记为通过；这不代表示波器电气项目已补测，也不代表第四阶段速度闭环通过。

## 14. 实车验收记录

| 日期/固件时间 | WAIT 静止 | 10 kHz | 左右正转 | 转向符号 | LOST 停车 | 10 圈 | 操作者 |
|---|---|---|---|---|---|---|---|
| 2026-08-01 / 后续回归 | 是 | 源码计算10 kHz | 是 | 是 | 是 | 连续10圈不丢线 | 用户 |
