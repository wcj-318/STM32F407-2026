# H题第三阶段：编码器采集与测速上板测试

> 阶段状态：第三阶段已经实测通过，本文保留H3固件的100 ms、16通道定义。当前第四阶段已经改为20 ms速度PI和18通道JustFloat，测试时不要混用H3/H4通道表或旧HEX。

## 1. 阶段目标与边界

本阶段建立第三阶段的编码器测量基线，只完成：

1. 使用 TIM2、TIM3 的硬件正交编码器模式采集左右轮 CHA/CHB。
2. 每 100 ms 计算左右轮计数增量和 counts/s。
3. 通过 USART1 输出累计计数、区间计数和计数速度。
4. 保留已经能够连续循迹 10 圈的硬件、供电和安全停车基线，并加入不依赖编码器的循迹稳定性修正。

本阶段明确不实现：

- 编码器速度 PI/PID；
- 根据编码器自动修改左右 PWM；
- 未经实测 PPR/CPR 的 RPM、轮速或里程换算；
- 起止线计圈、A 点自动停车和五路灰度停车；
- 丢线自动搜索和摆杆滚球机构控制。当前 300 ms 短时容错只保持最后一次有效转向，不属于自动搜索。

因此，本阶段固件下载成功或串口出现计数，不等于速度闭环完成。

## 2. 硬件依据和假设

本地硬件资料确认 MG513X 为霍尔增量式编码器，接口包含：

| 电机线 | 功能 |
|---|---|
| M+、M- | 电机驱动输出 |
| VCC、GND | 编码器供电和地 |
| CHA、CHB | A/B 两相正交信号 |

资料没有给出可直接验收的编码器 PPR/CPR，也没有明确说明参数是电机轴还是减速箱输出轴。因此本阶段只输出硬件计数，必须通过输出轴手动转动 10 圈实测后才能换算 RPM。

编码器信号接入 STM32 前必须确认：

1. 编码器 GND、STM32 GND、MD240A GND 共地。
2. CHA/CHB 高电平满足 STM32 输入要求；不能因为编码器使用 5 V 供电就假定信号一定可直接接入。
3. 不改变已经确认可工作的电瓶、电机和驱动供电。

## 3. 编码器引脚

采用用户确认的推荐引脚：

| 信号 | STM32F407 引脚 | 定时器功能 |
|---|---|---|
| 左轮 CHA | PA5 | TIM2_CH1，AF1 |
| 左轮 CHB | PB3 | TIM2_CH2，AF1 |
| 右轮 CHA | PC6 | TIM3_CH1，AF2 |
| 右轮 CHB | PC7 | TIM3_CH2，AF2 |

该分配不占用：

- 八路灰度 PE8～PE11；
- 五路灰度 PD8～PD12；
- 电机 PWM PD13/PD14；
- 电机方向和 STBY PC0～PC4；
- PA0 启动键；
- USART1 PA9/PA10；
- OLED PB8/PB9；
- SWD PA13/PA14；
- 后续机构预留的 PA6/PA7。

PC6/PC7 在当前工程中不能再调用 `UART6_Init()`；PB3 不再用于 SWO，但 PA13/PA14 的 SWD 下载调试保持不变。

## 4. 定时器配置

TIM2 和 TIM3 均配置为：

```text
EncoderMode  = TIM_ENCODERMODE_TI12
Period       = 0xFFFF
IC1/IC2      = rising edge, direct input
Prescaler    = DIV1
Input filter = 8
GPIO pull    = pull-up
```

`TIM_ENCODERMODE_TI12` 对 A/B 两相进行硬件正交解码。计数正负方向由 CHA、CHB 的相位顺序决定；左右电机镜像安装时，同为车辆前进方向的原始计数可能一正一负，这是接线方向信息，不在本阶段用软件掩盖。

16 位计数器会自动回绕。固件用相邻两次采样的 16 位差值计算有符号增量，可正确处理：

```text
65530 -> 5  = +11
5 -> 65530  = -11
```

## 5. 软件文件和数据流

| 文件 | 职责 |
|---|---|
| `Core/Inc/main.h` | 定义 PA5、PB3、PC6、PC7 编码器引脚 |
| `Core/Inc/tim.h`、`Core/Src/tim.c` | TIM2/TIM3 正交编码器初始化 |
| `Bsp/Encoder/encoder.h/.c` | 启动计数器、累计、回绕差分和 counts/s |
| `Core/Src/main.c` | 100 ms 采样、启动清零和 UART 输出 |
| `MDK-ARM/STM32F407.uvprojx` | Keil 工程加入 Encoder 目录和 `encoder.c` |
| `.eide/eide.yml` | EIDE 工程加入 Encoder 目录和 `encoder.c` |

当前 `STM32F407.ioc` 仍是原始 TIM1 示例模板，没有记录第一、第二阶段手工配置，也没有记录本阶段 TIM2/TIM3 和四根编码器引脚。现阶段不要使用 CubeMX 重新生成并覆盖工程；后续若同步 `.ioc`，必须一次性迁入全部已验收引脚后重新回归测试。

数据流：

```text
左 CHA/CHB -> TIM2 -> 16位计数 -> 100ms差分 -> ELT/ELD/ELCPS
右 CHA/CHB -> TIM3 -> 16位计数 -> 100ms差分 -> ERT/ERD/ERCPS
```

counts/s 使用实际采样间隔计算：

```text
counts_per_second = delta_count * 1000 / elapsed_ms
```

即使 WAIT/LOST 状态的显示任务令某次采样晚于 100 ms，速度也按串口中的实际 `EDT` 归一化。RUN 状态已经禁止 OLED 全屏刷新，不再由显示任务周期性阻塞 10 ms 循迹控制；后续启用速度闭环时仍应继续监测实际控制周期。

## 6. 状态与清零行为

OLED 和 UART 固件标识改为：

```text
H3 WAIT
H3 RUN
H3 LOST
```

编码器在 WAIT、RUN、LOST 三种状态下都保持采集，便于架空手动测试。

- 上电初始化时累计计数为 0。
- 每次有有效黑线并按 PA0 进入 RUN 时，将本次运行累计计数清零。
- 人工停车或进入 LOST 后不清零，保留停车和惯性阶段的最终计数。
- 下一次有效启动时重新从 0 开始。

编码器数据只用于显示，不参与 `Motor_SetSignedPermille()`，因此不会改变第二阶段循迹输出。

## 7. VOFA 通信格式

USART1 连接 JDY-31，当前实际配置为：

```text
PA9/PA10
9600-8-N-1
VOFA+：JustFloat（小端 float32 二进制）
每帧：16 个 float + 00 00 80 7F 帧尾
```

通道定义与 `Core/Src/main.c` 完全一致：

| VOFA 通道 | 含义 |
|---:|---|
| `I0` | 状态：0=WAIT，1=RUN，2=LOST |
| `I1` | 八路有效掩码 `gray8_mask` |
| `I2` | 八路不稳定掩码 `gray8_unstable_mask` |
| `I3` | `line_valid` |
| `I4` | `error10` |
| `I5/I6` | 左/右目标 PWM，千分比 |
| `I7` | PD 校正量，千分比 |
| `I8` | 五路原始掩码 |
| `I9` | 编码器实际测速间隔，ms |
| `I10/I11` | 左/右本周期增量 |
| `I12/I13` | 左/右 counts/s |
| `I14/I15` | 左/右累计计数 |

正负号只表示当前 CHA/CHB 相位方向，不表示已经完成左右方向校准。

## 8. 构建结果

本次流畅循迹修正后，Keil 与 EIDE 工程均已执行全量重编译：

```text
编译器：Arm Compiler 5.06 update 7 (build 960)
Keil 结果：0 Error，0 Warning
EIDE 结果：build successfully
程序大小：Code=13506，RO-data=726，RW-data=88，ZI-data=15880
```

本次构建实际包含：

```text
encoder.c
main.c
tim.c
gray.c
line_follow.c
motor.c
oled.c
bsp_UART.c
```

固件：

```text
MDK-ARM/STM32F407/STM32F407.hex
本次固定名称：MDK-ARM/STM32F407/STM32F407_H3_SMOOTH_TRACKING.hex
大小：40326 bytes
时间：2026-08-01 01:54:00
SHA-256：57345B850EBE7F7C991840CF36D22CB122D7C0474209D506B7294A2F69B56E39
```

EIDE 同步生成 `build/STM32F407/STM32F407.hex`，程序大小与 Keil 构建一致。上板统一使用上述带 `SMOOTH_TRACKING` 的固定名称；之前所有编码器采集、稳定性和窄线中心文件均不是本次流畅循迹固件，避免混用。

构建时保留的循迹参数：

```text
BASE=300
MAX=600
MIN_FORWARD=100
CURVE_BASE_MIN=240
CORRECTION_LIMIT=140
KP=2
KD=1
CENTER_ERROR10=10
CENTER_CORRECTION=20
CORRECTION_SLEW_PER_STEP=30
CONTROL_PERIOD=10ms
LOST_GRACE=30 frames (300ms)
```

这些是源码和构建产物验证，不是实物编码器验收。

### 8.1 头文件版本一致性修复记录

工程曾因 `Bsp/LineFollow/line_follow.h` 被旧版本覆盖而出现 15 个编译错误：旧头文件缺少校正限幅、丢线容错宏以及控制器的四个保持状态字段，但 `line_follow.c` 已经使用这些接口。修复时只恢复以下内容：

- `HLINE_MIN_FORWARD_PWM_PERMILLE=100`；
- `HLINE_CORRECTION_LIMIT_PERMILLE=200`；
- `HLINE_LINE_KD=2`；
- `HLINE_LOST_GRACE_FRAMES=30`；
- `last_left_pwm`、`last_right_pwm`、`last_correction`、`lost_frames`。

修复后 Keil 全量重编译为 `0 Error / 0 Warning`，EIDE 全量重编译为 `build successfully`。本次没有修改任何硬件引脚、电机方向、灰度通道顺序、编码器映射或供电参数。

### 8.2 运行态控制周期阻塞修复

原主循环每 100 ms 同时刷新 OLED 和发送 VOFA。VOFA 已由 USART1 TXE 中断发送，不阻塞主循环；问题来自 OLED 软件 I²C 全屏刷新：每行发送 128 字节，8 行的最低时序耗时约 60.5 ms，还没有计算 HAL GPIO 调用开销。一次刷新会漏掉约 6 个 10 ms 循迹控制周期，车辆位置误差因此跨通道跳变，容易令 PD 校正打满并丢线。

本次调度改为：

- RUN 状态完全禁止 OLED 全屏刷新，优先保证 10 ms 灰度采样和电机控制；
- WAIT/LOST 状态每 500 ms 刷新 OLED；
- VOFA 仍每 100 ms 排入 USART1 中断发送队列，16 通道和 9600 波特率不变；
- `BASE=300`、`KP=4`、`KD=2`、校正限幅和丢线保护均不变，确保本轮只验证调度根因。

因此车辆运行时 OLED 内容会暂时冻结，这是本版的预期行为，不是程序死机；红色状态灯和 VOFA 数据仍正常工作。停车或进入 LOST 后 OLED 会恢复更新。

### 8.3 窄线中央双传感器控制

窄黑线在中线附近通常只会命中 CH4（`error10=-10`）或 CH5（`error10=+10`），不一定同时命中两颗。完全把 CH4/CH5 设为零校正虽然能消除反打，但车辆会在中央区域内自由漂移，直到碰到 CH3/CH6 才开始修正，不符合“直线尽量保持在中间两颗”的要求。

当前控制改为：

- CH4 单独命中：固定小校正 `-20‰`，左右目标约 `280/320‰`；
- CH5 单独命中：固定小校正 `+20‰`，左右目标约 `320/280‰`；
- CH4+CH5 居中：校正 0，左右目标 `300/300‰`；
- 中央两颗切换保持 ±20‰ 小修正；进入外侧后，相邻两个 10 ms 控制周期的校正量最多变化 30‰；
- 当前最外侧校正限幅为 ±140‰，弯中动态基准最低 240‰；详细连续降速逻辑见下一节。

CH4 与 CH5 之间切换时校正只在 `-20/+20‰` 之间变化，远小于旧版 `-80/+80‰`；外侧校正的进一步平滑和弯道降速见下一节。

### 8.4 连续弯道降速与柔顺转向

实车出现“转向力度很大但偶尔仍转不过”的原因，是旧控制在中等误差时就快速达到 `100/500‰`：外轮速度过高，车辆会猛甩并冲过弯线，而不是沿弯线稳定转动。

本次采用连续调速，不按通道硬切速度：

- `KP/KD` 从 4/2 调整为 2/1，降低误差跳变冲击；
- 外侧校正变化每 10 ms 最多 30‰；中央 CH4/CH5 的 ±20‰ 小修正不受此限制；
- 校正从 20‰ 增加至 140‰ 时，弯中基准 PWM 连续从 300‰ 降至 240‰；
- 最大校正为 140‰，因此最急弯约为 `100/380‰`，不再使用 `100/500‰` 猛甩；
- 内轮在所有有效线型下不低于 100‰，避免软件命令直接停轮。

控制序列验算：

| 稳态位置 | 校正量 | 动态基准 | 快轮/慢轮目标 |
|---|---:|---:|---:|
| CH4/CH5 | 20‰ | 300‰ | 320/280‰ |
| 约 `error10=30` | 60‰ | 280‰ | 340/220‰ |
| 约 `error10=50` | 100‰ | 260‰ | 360/160‰ |
| CH1/CH8 | 140‰ | 240‰ | 380/100‰ |

校正方向变化时先经过中间值，动态基准也随实际校正连续变化，因此不会在直线/弯道切换时突然整体加减速。

## 9. 分步上板测试

所有电机运行测试必须先架空车轮。

### 9.1 断电接线

1. 断开动力电源。
2. 左轮 CHA/CHB 分别接 PA5/PB3。
3. 右轮 CHA/CHB 分别接 PC6/PC7。
4. 确认编码器供电和公共地。
5. 确认没有把 M+、M- 接到 STM32。
6. 确认 PC6/PC7 没有同时接 USART6。

通过标准：无短路、无引脚复用、四根信号线与表格一致。

### 9.2 下载与固件身份

优先下载第 8 节的固定名称 HEX，复位后确认：

```text
OLED：H3 WAIT
VOFA I0：0（WAIT）
```

若运行时 OLED 保持最后一帧但 VOFA 持续更新，这是本版的正常调度；若仍显示 `H2`，说明下载的不是本阶段固件。

### 9.3 静止噪声

保持两个车轮完全静止 5 秒：

```text
ELD=0
ERD=0
ELCPS=0
ERCPS=0
```

如果静止时持续出现计数，检查公共地、CHA/CHB 悬空、信号电平和电机线干扰，不要先加入软件死区。

### 9.4 左右通道独立性

保持 `H3 WAIT`，只手动转动左轮：

- `ELT/ELD/ELCPS` 必须变化；
- `ERT/ERD/ERCPS` 必须保持 0。

反向转动左轮，`ELD/ELCPS` 的符号必须反转。

随后只转动右轮，要求完全对称。若转左轮却是右字段变化，交换左右编码器信号组，不修改程序名称掩盖接线。

### 9.5 输出轴每圈计数标定

分别对左右输出轮做两次测试：

1. 在轮胎和车架上做好起点标记。
2. 记录起始累计计数。
3. 沿同一方向缓慢、准确转动输出轮 10 圈。
4. 记录最终累计计数。
5. 计算：

```text
左输出轴 counts/rev = abs(左终值 - 左初值) / 10
右输出轴 counts/rev = abs(右终值 - 右初值) / 10
```

重复两次的同侧结果应基本一致。左右值不要强行取相同值，先记录实测结果。

| 测试 | 左10圈计数 | 左counts/rev | 右10圈计数 | 右counts/rev |
|---|---:|---:|---:|---:|
| 第1次 |  |  |  |  |
| 第2次 |  |  |  |  |

### 9.6 架空正转测速

1. CH4、CH5 对准黑线。
2. 架空车轮后按 PA0，确认 `H3 RUN`。
3. 记录连续 2 秒的 `L/R`、`ELCPS/ERCPS`。
4. 再按 PA0 停止。

通过标准：

- 两侧 counts/s 均非 0；
- 同一侧连续采样符号稳定；
- PWM 固定时 counts/s 没有频繁归零或大幅跳变；
- 停止后累计计数保留；
- 下一次有效启动累计计数从 0 重新开始。

左右原始符号可以不同，必须先记录：

| 动作 | 左计数方向 | 右计数方向 |
|---|---:|---:|
| 车辆前进 |  |  |
| 车辆后退/手动反转 |  |  |

### 9.7 第二阶段回归

编码器四项测试通过后才允许落地：

1. 复核八路 `G/U/E`。
2. 复核 `L/R/C` 与上一版一致。
3. 先跑直线、缓弯、急弯。
4. 连续运行 10 圈。

通过标准：编码器采集不造成新增 LOST，不改变转向符号，不降低已经通过的循迹可靠性。

### 9.8 丢线与中心稳定性修正验收

当前稳定版不修改电瓶、引脚、灰度通道顺序、编码器映射和电机方向。它保留前述 300 ms 短时丢线保护，并加入运行态 OLED 隔离、中央双传感器小力度闭环和校正变化限速。

控制逻辑如下：

- 直线基础 PWM 为 300‰，`KP=2`、`KD=1`；转向加深时动态基准最低降至 240‰。
- CH4/CH5 单独命中时，`I4` 显示 -10/+10，`I7` 分别约为 -20/+20。
- 外侧相邻控制周期的校正量最多改变 30‰，避免左右目标突然反打。
- 校正量限制为 ±140‰。CH1/CH8 稳定后约为 `100/380‰` 或 `380/100‰`，不会把内轮降为 0，也不会让外轮冲到 500‰。
- 灰度暂时无有效黑线时，前 30 个 10 ms 控制周期保持最后一次有效的左右 PWM 和校正量；期间重新识别黑线则立即恢复正常 PD 计算。
- 第 31 个连续无效周期仍未恢复时，才返回无效并由主状态机进入 `H3 LOST`、关闭电机。该保护用于跨越短边缘抖动或约 5 cm 的小缺口，不改变长时间真正丢线的停车行为。

架空测试：

1. 进入 `H3 RUN`，在 CH4、CH5 之间来回移动黑线；CH4 时 `I7≈-20、I5/I6≈280/320`，CH5 时 `I7≈+20、I5/I6≈320/280`，双中心时 `I7=0、I5/I6≈300/300`。
2. 依次只覆盖 CH1、CH8；稳定约 100 ms 后，`I5/I6` 应约为 `380/100` 或 `100/380`，运行侧不应为 0。
3. 先保持一个有效通道，再快速移开黑纸并在 300 ms 内恢复；状态应保持 RUN，电机不应瞬间停转。
4. 持续移开黑纸超过 300 ms；第 31 个无效控制周期后应进入 LOST，左右 PWM 归零。

落地测试：

1. 先跑直线，确认 `I4` 主要保持在 -10/+10，`I7` 只做约 -20/+20 的小修正，车身不再持续大幅反打。
2. 再跑缓弯和急弯，确认校正逐步增加、两轮整体速度随弯度下降，并且 CH1/CH8 能把车辆平顺拉回线路。
3. 连续运行 10 圈，记录是否出现 LOST，以及问题发生前 2 秒的 `I0～I7`。
4. 若仍有大幅摆动或急弯转不过，保留本版数据后一次只调整一个参数，不得同时改 KP、KD、中心校正和限速。

## 10. 第三阶段采集通过标准

以下项目全部满足，才能进入速度 PI/PID：

- 左右编码器独立计数，无串线；
- 静止时增量和 counts/s 为 0；
- 正反转时同一侧计数符号相反；
- 输出轴 10 圈计数能够重复；
- 架空固定 PWM 时 counts/s 连续稳定；
- 已记录车辆前进时左右原始计数符号；
- 第二阶段连续 10 圈回归通过；
- 已确认当前实际下载的是本阶段 HEX。

速度闭环开始前还必须完成：

1. 根据 10 圈实测确定左右 counts/rev。
2. 明确速度控制周期。
3. 保持 RUN 状态禁用 OLED 全屏刷新的调度约束，并监测速度闭环实际周期。
4. 先单轮速度闭环，再双轮同步，最后才接回灰度外环。

## 11. 实测记录

| 日期/固件哈希 | 静止为0 | 左右独立 | 正反符号 | 左counts/rev | 右counts/rev | 固定PWM稳定 | 10圈回归 | 操作者 |
|---|---|---|---|---:|---:|---|---|---|
| 2026-08-01 / H3实测数据 | 是 | 是 | 左前进正、右前进负；反转相反 | 1468.95 | 1471.20 | 是；左2324.67、右原始-2318.67 counts/s | 顺时针连续10圈不丢线 | 用户 |

实测依据：左轮两次十圈为14716、14663计数，重复差0.361%；右轮两次十圈绝对值为14698、14726计数，重复差0.190%。架空固定PWM末尾30帧中，左速度2320～2330 counts/s，右原始速度-2330～-2310 counts/s，采样周期均为100 ms。静止截图缓存344点且末尾26帧增量、速度均为0；顺时针截图缓存2697点，记录末尾保持RUN。
