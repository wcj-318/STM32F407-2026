# H题第四阶段：编码器速度 PI 闭环与上板测试

> 后续状态：当前工程已在LINE_TRACK速度PI基础上加入H题第二问的五路启停线停车和PA0启动计时。最终上板固件、VOFA 19通道定义及验收步骤以 `H题_第二问_PI闭环循迹_五路停车与计时_开发测试文档.md` 为准；本文件保留为速度PI分阶段测试记录。

> 当前入口：第四阶段代码功能已直接切换到最终 `LINE_TRACK`：八路灰度外环给定左右目标、两个编码器速度PI内环调节PWM。当前源码和最新通用 `STM32F407.hex` 均为LINE_TRACK；目录中的分模式HEX属于此前归档，本轮以重新编译的Keil/EIDE通用产物为准。代码与构建完成不等于实车验收完成。

## 1. 阶段结论与边界

本阶段在第三阶段编码器采集通过的基础上，实现：

1. 左轮独立速度 PI；
2. 右轮独立速度 PI；
3. 双轮固定目标速度同步；
4. 灰度外环给定左右目标速度，编码器内环调节实际 PWM；
5. VOFA 同时输出目标速度、原始速度、滤波速度和实际 PWM。

本阶段不实现五路停车线、横线状态机、编码器距离补偿和精准停车。这些功能必须在速度内环实车通过后再加入。

本次没有修改电瓶、电源、引脚、灰度通道顺序、电机方向或编码器接线。

## 2. 硬件映射保持不变

| 信号 | STM32F407 引脚 | 外设 |
|---|---|---|
| 左电机 PWM | PD13 | TIM4_CH2，10 kHz |
| 右电机 PWM | PD14 | TIM4_CH3，10 kHz |
| 左电机 IN1/IN2 | PC0/PC1 | GPIO |
| 右电机 IN1/IN2 | PC2/PC3 | GPIO |
| STBY | PC4 | GPIO |
| 左编码器 CHA/CHB | PA5/PB3 | TIM2 |
| 右编码器 CHA/CHB | PC6/PC7 | TIM3 |
| 启动键 | PA0 | 高电平有效，EXTI0上升沿 |
| JDY-31 UART1 | PA9/PA10 | 9600-8-N-1 |
| OLED | PB8/PB9 | 软件 I2C |

车辆前进时，第三阶段实测的原始编码器符号为：

```text
左轮：正数
右轮：负数
```

速度控制内部把右轮负号转换为正数，因此第四阶段 VOFA 的左右前进速度均显示为正。

## 3. 第三阶段实测参数

输出轴两次十圈标定：

| 参数 | 实测值 |
|---|---:|
| 左轮 counts/rev | 1468.95 |
| 右轮 counts/rev | 1471.20 |
| 300‰ PWM 左轮参考速度 | 2325 counts/s |
| 300‰ PWM 右轮参考速度 | 2319 counts/s |
| 300‰ PWM 左右 RPM | 94.95 / 94.56 RPM |

左右轮分别保留独立标定值，不强行使用同一个常数。

## 4. 控制结构

```text
八路灰度
   ↓ 10 ms
线路误差和左右开环请求 PWM
   ↓ 按实测 300‰ 参考点换算
左右目标 counts/s
   ↓ 20 ms
编码器速度 PI + 抗积分饱和 + 输出斜率限制
   ↓
实际左右 PWM
   ↓
TB6612 / MD240A
```

固定速度模式不使用灰度外环，直接给定 300‰ 的速度参考，用于隔离检查速度 PI。

目标速度换算：

```text
左目标 counts/s = 左请求 PWM × 2325 / 300
右目标 counts/s = 右请求 PWM × 2319 / 300
```

PI 输出不是直接代替原 PWM，而是在原请求 PWM 上增加闭环修正：

```text
实际 PWM = 请求 PWM + Kp × 速度误差 + Ki × 误差累计
```

## 5. 当前速度环参数

参数集中在 `Bsp/Motor/motor.h`：

| 宏 | 当前值 | 含义 |
|---|---:|---|
| `HLINE_SPEED_CONTROL_PERIOD_MS` | 20 ms | 编码器采样和 PI 周期 |
| `HLINE_SPEED_KP_NUMERATOR/DENOMINATOR` | 1/10 | Kp=0.1 PWM/(count/s) |
| `HLINE_SPEED_KI_DENOMINATOR` | 2000 | 每周期积分换算系数 |
| `HLINE_SPEED_CORRECTION_LIMIT_PERMILLE` | 150‰ | PI 最大附加修正 |
| `HLINE_SPEED_OUTPUT_SLEW_PER_STEP` | 20‰ | 每20 ms最大 PWM 变化 |
| `HLINE_SPEED_FILTER_DIVISOR` | 4 | 速度一阶滤波 |
| `HLINE_SPEED_OUTPUT_MAX_PERMILLE` | 600‰ | 全局输出硬上限；固定300‰测试受±150‰修正限制，实际调节范围为150～450‰ |

PI 修正饱和时停止继续向同方向积分；停车时清零滤波器、积分器和输出，不保留上一次运行的积分。

当前灰度外环 `Kp=5、Kd=7` 是进入第四阶段前工程中的现值，本次没有修改。

## 6. 调度周期

| 任务 | 周期 | 说明 |
|---|---:|---|
| 灰度读取和目标更新 | 10 ms | 仅循迹模式参与目标变化 |
| 编码器采样和速度 PI | 20 ms | 50 Hz |
| VOFA 帧 | 100 ms | 受 9600 bps 带宽限制 |
| OLED | 上电和启停切换立即刷新；WAIT/LOST时500 ms | RUN期间禁止连续全屏刷新 |

OLED 软件 I2C 全屏刷新约需60 ms，因此启动前先显示一次RUN状态，车辆运行期间保持冻结；按键停止后立即显示WAIT，避免破坏20 ms速度控制周期。

板载KEY1按下时PA0为高电平，EXTI0上升沿中断只累计原始边沿，主循环负责切换RUN/WAIT；80 ms保护时间屏蔽异常重复边沿。VOFA通过USART1发送中断队列输出，RUN期间不刷新OLED全屏；长按只有一次有效上升沿，不会反复启停。

## 7. 四种固件模式

模式由 `Bsp/Motor/motor.h` 的 `HLINE_SPEED_RUN_MODE` 选择。当前工程已经切换到第四阶段最终循迹模式；首次下载仍应先架空确认双轮方向，再落地测试。

| 模式 | OLED | 功能 | 当前测试产物 |
|---|---|---|---|
| 左轮 | `H4L WAIT/RUN` | 左轮300‰闭环，右轮强制0 | 已取得有效RUN数据；当前通用HEX已不再是左轮模式 |
| 右轮 | `H4R WAIT/RUN` | 右轮300‰闭环，左轮强制0 | 已取得1.16秒有效RUN数据；当前通用HEX已不再是右轮模式 |
| 双轮 | `H4D WAIT/RUN` | 双轮300‰闭环 | 左右单轮通过后切换宏并重新编译 |
| 循迹 | `H4T WAIT/RUN/LOST` | 灰度外环 + 双轮速度内环 | 当前 `STM32F407.hex`，已经由当前源码重编译 |

必须按左轮、右轮、双轮、循迹顺序测试，不要直接从循迹固件开始。

## 8. VOFA 配置和通道

```text
协议：JustFloat
串口：JDY-31 / COM口
波特率：9600
数据位：8
校验：None
停止位：1
流控：None
每帧：18个小端float32 + 00 00 80 7F
```

| 通道 | 含义 |
|---:|---|
| `I0` | 状态：0=WAIT，1=RUN，2=LOST |
| `I1` | 八路有效掩码 |
| `I2` | 八路不稳定掩码 |
| `I3` | `line_valid` |
| `I4` | 灰度 `error10` |
| `I5/I6` | 左/右目标 counts/s |
| `I7` | 灰度外环校正量；固定模式为0 |
| `I8` | 五路原始掩码，仅观察 |
| `I9` | 编码器实际采样周期，ms |
| `I10/I11` | 左/右未滤波实测速率，已统一为前进正数 |
| `I12/I13` | 左/右 PI 使用的滤波速度 |
| `I14/I15` | 左/右实际输出 PWM，千分比 |
| `I16` | PA0原始电平：松开为0，按下为1 |
| `I17` | PA0上升沿中断累计次数，每次有效按下至少增加1 |

第三阶段的累计计数不再占用 VOFA 通道；累计计数仍由编码器模块维护，后续距离停车阶段再使用。

## 9. 构建与固件

Keil当前LINE_TRACK最终模式已经由当前源码全量重编译，结果：

```text
0 Error(s), 0 Warning(s)
Code=14102
RO-data=726
RW-data=100
ZI-data=15924
```

速度 PI 实现在两个工程原本就共同编译的 `Bsp/Motor/motor.c` 中，避免 EIDE 自动源文件列表遗漏新模块。

| 当前有效固件 | SHA-256 |
|---|---|
| Keil：`MDK-ARM/STM32F407/STM32F407.hex`（LINE_TRACK） | `C724F3A578815D640460DEA0D4CB3EA8F8F4A165249931E936E187B0F1DC6DF4` |
| EIDE：`build/STM32F407/STM32F407.hex`（LINE_TRACK） | `6EA6E78896EBD10E7F8468D432BD7EC18335DA9A2317FD49B4BA3BEE12761099` |

两个构建入口使用同一份应用源码且均构建成功，但编译选项不同，HEX不是逐字节相同；本轮下载优先使用上表Keil路径并记录对应哈希。目录中带 `LEFT/RIGHT/DUAL/LINE` 名称的文件均按历史快照处理，不用旧LINE文件替代本次新生成的通用HEX。后续每切换一种模式，都必须从当前源码重新编译、确认OLED前缀并记录新哈希。

## 10. 上板测试步骤

### 10.1 通用安全检查

1. 两个驱动轮全部离地，断开后重新确认电机线和编码器线。
2. 不更换电瓶电压，不改变TB6612接线。
3. 打开VOFA，只观察本节规定通道。
4. 每次下载后先确认OLED模式字符，再按板载KEY1（PA0）。
5. 出现反转、卡死、持续达到当前模式输出上限或异响时立即按板载KEY1并断开动力电源。

PA0单独验收：上电松开按键时`I16=0`；按住KEY1时`I16=1`，`I17`增加；第一次按下后`I0=1`，第二次按下后`I0=0`且`I14/I15=0`。如果`I16`变化但`I17`不增加，检查EXTI0中断；如果`I16`始终为0，检查KEY1、PA0和3.3 V硬件连接。

### 10.2 左轮闭环（已取得有效RUN数据）

左轮模式曾按以下通道完成测试；当前通用HEX已经切换为右轮模式，不要再按本小节判断当前固件：

```text
上电：H4L WAIT
按一次板载KEY1：H4L RUN
I5约2325，I6=0
I14从20开始逐步上升，I15=0
I10/I12逐步接近2325
```

连续记录5秒。通过标准：

- 启动后的首个样本可因一次OLED状态刷新出现约80 ms，随后 `I9` 应主要为20 ms；
- 右轮 `I6/I11/I13/I15` 始终为0；
- 1秒后左滤波速度 `I12` 进入目标的±10%；
- 2秒后进入目标的±5%，即约2209～2441 counts/s；
- 启动后 `I14` 不得持续为0；固定300‰模式的实际PI上限是450‰，不得长期顶在450而速度仍达不到目标；
- 速度没有周期性大幅振荡或频繁归零；
- 数据必须在左轮仍处于RUN时保存，不能只复制第二次按键后的终端末尾；
- 再按板载KEY1后 `I14=0`，车轮停止。

### 10.3 右轮闭环（已取得短时有效RUN数据）

右轮模式曾按以下通道完成短时测试；当前通用HEX已经切换为循迹模式，不要再按本小节判断当前固件：

```text
上电：H4R WAIT
按一次板载KEY1：H4R RUN
I5=0，I6约2319
I14=0，I15逐步上升
I11/I13显示为正数并接近2319
```

连续记录10秒。通过标准与左轮对称：左轮 `I5/I10/I12/I14` 始终为0；`I6`约2319；`I11/I13`为正数；2秒后的±5%范围约为2203～2435 counts/s；`I15`不得持续为0，也不得长期顶在450而速度仍达不到目标；第二次按KEY1后 `I15=0`。

### 10.4 双轮固定速度闭环

左右单轮都通过后，将模式切换为 `HLINE_SPEED_MODE_DUAL_FIXED` 并重新编译：

```text
上电：H4D WAIT
按一次板载KEY1：H4D RUN
I5约2325，I6约2319
I7=0
```

连续记录10秒。通过标准：

- 左右滤波速度分别在各自目标±5%内；
- 左右归一化速度差不超过目标平均值的5%；
- 两轮都不反转，不出现某一侧持续为0，或长期顶在固定300‰模式的450‰修正上限而速度仍达不到目标；
- PWM允许不同，因为闭环需要补偿电机差异；
- 按键停止后两路实际PWM均为0。

### 10.5 循迹速度闭环

当前源码已直接切换为 `HLINE_SPEED_MODE_LINE_TRACK` 并重新编译。首次运行先架空，让八路灰度保持识别黑线，按KEY1确认两个轮子都按车辆前进方向转动；方向、启停和速度通道正确后再落地：

1. 先跑直线，记录 `I4、I5/I6、I10/I11、I14/I15`。
2. 再跑缓弯，确认目标速度变化后实测速度跟随。
3. 最后跑急弯和连续十圈。

判断问题来源：

- `I5/I6` 自身频繁从高到低反向跳变：灰度外环问题；
- `I5/I6` 稳定，但 `I12/I13` 大幅振荡：速度 PI 问题；
- 目标和速度都稳定，车仍偏转：检查轮径、载荷、机械阻力和传感器安装。

不要在同一轮测试中同时修改灰度 Kp/Kd 和速度 Kp/Ki。

## 11. PI 调节顺序

只有固定速度模式不通过时才调整速度 PI：

1. 若速度快速来回振荡：把 `HLINE_SPEED_KP_DENOMINATOR` 从10增大到15或20。
2. 若稳定后长期存在偏差：把 `HLINE_SPEED_KI_DENOMINATOR` 从2000逐步减小，但每次只改一个档位。
3. 若启动输出变化过猛：减小 `HLINE_SPEED_OUTPUT_SLEW_PER_STEP`。
4. 若启动过慢且没有振荡：适当增大斜率值。
5. 不要通过修改 counts/rev、电瓶电压或交换编码器符号来掩盖 PI 问题。

每次修改后必须重新完成对应单轮和双轮固定速度测试。

## 12. 第四阶段通过标准

以下全部满足后，才可把速度闭环作为后续停车控制的基础：

- 左轮独立速度闭环通过；
- 右轮独立速度闭环通过；
- 双轮固定目标速度连续10秒稳定；
- 目标速度、滤波速度和实际PWM均有完整VOFA记录；
- PA0启停可靠，停止后PWM为0；
- 循迹模式不丢线，速度内环没有引入新的振荡；
- 已记录实际使用的固件哈希和PI参数。

| 固件哈希 | 左轮±5% | 右轮±5% | 双轮差≤5% | 停止为0 | 循迹10圈 | 操作者/日期 |
|---|---|---|---|---|---|---|
|  |  |  |  |  |  |  |

## 13. 当前验证状态

已完成：第三阶段编码器采集验收；板载KEY1/PA0启停；左轮RUN截图显示 `I5=2325、I10=2300、I12=2306、I14=331`，单点误差小于1%；右轮TXT共13帧、约1.16秒，全部RUN，`I6=2319、I11=2100～2350、I13=2172～2327、I15=316～331、I9=20`，平均滤波误差约-2.83%，证明右轮闭环方向和调节动作有效。灰度外环、双轮速度PI内环、LOST停机和PA0启停代码均已接通；当前LINE_TRACK最终工程已完成Keil和EIDE全量重编译。

未完成：左轮缺10秒连续曲线，右轮仅1.16秒且13帧中8帧进入±5%；尚无双轮固定速度记录和最终H4T整车循迹记录。因此“第四阶段功能实现与可烧录固件”已完成，“第四阶段实车验收”尚未完成。下载后必须按第10.1、10.4、10.5节完成架空与落地验证。
